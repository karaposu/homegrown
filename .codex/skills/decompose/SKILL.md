---
name: decompose
description: Structural Decomposition
---

---
name: decompose
description: Perceives the internal coupling topology of a complex whole and partitions it into tractable, independently coherent pieces with explicit interfaces and dependency ordering. Use when a problem is too large for one focused pass, when a whole has internal sub-problems that need separate treatment, when a question tree needs to be produced from a complex topic, or before /innovate or /td-critique on a problem with visible internal structure.
---

# /decompose — Structural Decomposition

## Step 0 — Mandatory pre-read

**Before reading anything else in this file, read `references/decompose.md` in full.** The 7-step process below references concepts — coupling perception, the coupling map, boundaries, pieces-as-questions, interfaces, dependency order, the seven self-evaluation dimensions, and the seven failure modes — that are defined ONLY in that file. Skipping this read produces shallow output that misses the discipline's actual mechanism.

Do not proceed to Step 1 until the read completes.

---

Analyze the given input using the Structural Decomposition Framework loaded in Step 0. Decomposition is not dividing — it's perceiving where the natural boundaries are so the cutting is obvious.

## Additional Input/Instructions

$ARGUMENTS

---

## Instructions

1. Read the input and consume it. It can be raw text, a folder path with md files, code files, or image path. Consume all input.

2. Execute the full Structural Decomposition process described in `references/decompose.md` — the 7-step sequence (Perceive Coupling Topology → Detect Boundaries Top-Down → Validate Boundaries Bottom-Up → Express as Question Tree → Map Interfaces → Order by Dependency → Self-Evaluate) — producing the coupling map, question tree with verification criteria, interface map, dependency order, and self-evaluation.

3. Save the output as a markdown file (unless differently stated in additional instructions!):
   - **If the input was a file path** — save in the same folder as the input file or relevant files.
   - **Otherwise** — save under `devdocs/decomposition/<suitable-name>.md` (create the directory if needed).

---

**Reference loading during execution.** When recognizing failure modes (premature decomposition, wrong boundaries, hidden coupling, missing pieces, over-decomposition, ignoring dependencies, imbalanced decomposition), consult the "Failure Modes" section of `references/decompose.md` for full descriptions and corrective actions. The framework's vocabulary (coupling gradients, interface flow types, the 3-vs-7 self-evaluation dimensions, stopping criteria) is canonically defined in that file.
