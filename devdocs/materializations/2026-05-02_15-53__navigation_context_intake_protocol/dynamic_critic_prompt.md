# Dynamic Critic Prompt: navigation_context_intake_protocol

Evaluate the plan to create `homegrown/protocols/navigation_context_intake.md`.

The artifact is a new Homegrown protocol. It is not wired into a runner yet, but future Navigation work may load it.

Assess whether the plan:

- preserves the source authority from the two findings;
- keeps Navigation context intake separate from Navigation enumeration;
- avoids creating a new discipline too early;
- avoids creating standalone project-state warm-up commands too early;
- reuses existing codebase archaeology commands only for the codebase profile;
- defines a non-code project/artifact/thinking-state track without calling it business-level by default;
- defines Compact, Standard, Deep, and Full modes clearly;
- defines the staged unlock pattern clearly;
- produces a usable current-state brief contract;
- records read set, missing context, trace, and after-use review gate;
- avoids broad write-set creep;
- does not touch `.codex`;
- remains concrete enough to be usable, not just conceptual.

For each issue, provide severity, impact, affected area, and mitigation.
