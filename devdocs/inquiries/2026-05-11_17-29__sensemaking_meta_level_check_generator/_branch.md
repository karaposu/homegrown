# Branch: Sensemaking Meta-Level Check Generator

## Question

Sensemaking currently has SPECIFIC perspective checks, refinement notes, and load-bearing concept tests, each added when a particular failure mode was diagnosed (the 14-00 finding's deferred Layer 2 would add two more — Question Premise Check before SV1; Cross-Candidate Unity check at Phase 2). Instead of growing this list rigidly check-by-check, what **meta-level question (or small set of meta-questions)** would GENERATE specific Sensemaking checks at runtime — such that the convergence-recognition check ("wait, aren't they doing the same thing?"), the Frame-exit Completeness check, the Question Premise Pre-Bias check, and other known checks would all emerge as INSTANCES of one underlying meta-operation when the meta-question is applied to the inquiry's content?

## Goal

Identify the meta-level question(s) — or the structural principle that generates them — that would produce the family of Sensemaking checks as runtime instances rather than as a hard-coded enumeration. A good answer would:

1. State the meta-question(s) clearly, in plain language a Sensemaking practitioner could apply.
2. Demonstrate coverage: do the meta-questions actually generate the known check instances (convergence-recognition; Frame-exit Completeness; Question Premise Pre-Bias; Load-bearing Concept Test; Cross-Candidate Unity; etc.)?
3. Demonstrate discoverability: can a Sensemaking practitioner (human or AI) APPLY the meta-questions to an inquiry's content and reliably surface the instance checks?
4. Identify the tier ceiling: meta-questions should remain a discipline-layer operation (not collapse into runner-layer cognition or expand into a new discipline).
5. Propose how Sensemaking's spec at `homegrown/sense-making/references/sensemaking.md` could be REORGANIZED around the meta-question(s), if reorganization is warranted.

The user's exact framing: *"sensemaking checks shuldnt be so rigid.. we sohuld define them as meta so they have coverage.. what meta level question would create a question 'wait, arent they doing the same thing?'"*

## Scope Check

Question covers goal: YES. The question asks what meta-question would generate specific checks like convergence-recognition; the goal compiles the meta-question(s), coverage demonstration, discoverability, tier ceiling, and reorganization proposal.

**Specific-vs-pattern check:** the user named a specific case (the convergence-recognition check). The broader pattern is "Sensemaking checks in general — can a generative meta-question replace the rigid enumeration?" Default reading: address the broader pattern; the convergence-recognition example is one INSTANCE the meta-question must generate.

**Question Premise Pre-Bias check:** the user's framing pre-biases toward "yes, a meta-question exists and is better than the enumeration." Inverse framing: "no, the enumeration is structurally appropriate; specific checks should stay specific." Both directions must be tested. The inverse argument: enumerated checks are more discoverable than abstract meta-questions; meta-questions risk becoming vague-and-useless.

**Self-reference acuity:** HIGH. This inquiry uses Sensemaking to investigate Sensemaking's check design. The disciplines analyzing Sensemaking's check architecture will be running BY Sensemaking. Mitigations required: external anchoring (the current Sensemaking spec at `homegrown/sense-making/references/sensemaking.md`; the 14-00 finding's deferred Layer 2 design; concrete known check instances); FEC meta-application; track whether Sensemaking stays within mandate while running on itself.

## Relationships

- CONTINUES FROM: `devdocs/inquiries/2026-05-11_14-00__loop_diagnose__why_disciplines_missed_convergence/finding.md` — the 14-00 finding's deferred Layer 2 added two specific checks; this inquiry asks whether a meta-question would have generated them instead.
- CONTINUES FROM: `devdocs/inquiries/2026-05-11_17-00__mvl_input_rephrase_as_primary_intervention/finding.md` — the 17-00 finding's runner-layer pre-bias check is upstream-layer; this inquiry investigates the discipline-layer meta-architecture.
- POTENTIALLY AFFECTS: `homegrown/sense-making/references/sensemaking.md` (the discipline spec — may be reorganized around meta-question(s) if viable).
- RELATED: 13-30 / 13-45 correction chain (the origin case for convergence-recognition failure); prior discipline-architecture inquiries (12-30 Sensemaking one or multiple; 09-20 Frame-exit Completeness meta-categories).
