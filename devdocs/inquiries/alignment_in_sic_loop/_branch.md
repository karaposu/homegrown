# Branch: Alignment in SIC Loop

## Question
Is the scope problem (narrow question → narrow answer) actually a Task Alignment problem (Layer 2 of the six alignment layers), and should the SIC loop incorporate alignment checking across all 6 layers — with the specific proposal being a pre-SIC alignment adjustment step where the system checks the input against all 6 layers, adjusts the task definition, and asks the user to confirm before S starts?

## Goal
Determine: (1) whether the scope problem maps to alignment layers, (2) whether ALL 6 layers should be checked (not just scope/Layer 2), (3) WHERE the alignment check should live (before SIC, in C, or both), (4) whether the pre-SIC alignment adjustment with user confirmation is the right design, and (5) how this connects to the briefing, adapters, and the build sequence already established.

## Scope Check
Question covers goal: YES — asks about mapping to alignment, all 6 layers, placement, pre-SIC design, and system integration.
