# Branch: Skill Invocation in Auto-Chain

## Question
Can the AI programmatically invoke the Skill tool to load discipline specs during MVL+ auto-chaining, how can we verify it fires vs. the AI pantomiming from memory, and what does this mean for the auto-chain design?

## Goal
A tested answer (not theoretical) about whether the Skill tool can be self-invoked by the AI, what the observable difference is between Skill-loaded execution and memory-based imitation, and how the MVL+ auto-chain spec should handle spec loading to guarantee faithful discipline execution.

## Scope Check
Question covers goal. The three sub-questions (can it invoke, how to verify, what it means for design) together produce the tested answer and the spec guidance the goal requires.
