# Branch: Why did Sensemaking application miss Memory disambiguation?

## Question

Given the prior loop_diagnose finding (`devdocs/inquiries/2026-05-09_21-15__loop_diagnose__memory_ambiguity_in_metaloop_ladder/finding.md`) which identified that Sensemaking's load-bearing concept test in `homegrown/sense-making/references/sensemaking.md` Phase 3 contains the proxy-vs-structural sub-test designed exactly for "is this label one structural distinction or several lumped?", and given that this test was applied to other concepts (5 cognitive roles, autonomy ladder, movement direction, loop count) but NOT applied to Memory in the prior MVL+ run on the metaloop ladder — **what was the primary causal mechanism that prevented the test from firing on Memory?** Was it: (a) categorical pre-capture (Memory was treated as a structural axis-name not a concept-to-test), (b) status quo bias (Memory was inherited from prior findings as already-vetted), (c) anchor dominance (the multi-axis frame was too strong an anchor for everything to organize under), (d) trigger-language abstractness (the spec's "concept whose use depends on runtime determination" required a frame-independent view the loop couldn't take), (e) procedural-scope misinterpretation (the loop scoped "load-bearing concepts" to commitments-being-made, not to terms-within-commitments), (f) some combination?

## Goal

A single sharp answer naming the primary causal mechanism — with evidence grounded in the prior `docarchive/sensemaking.md` text — plus the secondary contributing mechanisms ranked. The user must be able to:
1. Read one paragraph and understand WHY the test didn't fire (causal mechanism, not just description).
2. See which of the candidate mechanisms (a)-(f) is primary, with reasoning.
3. Use the answer to evaluate whether M1 (the pre-CONCLUDE checklist) actually closes the causal mechanism, or whether a different maintenance candidate is needed.

The goal is NOT to produce another full maintenance proposal — the prior loop_diagnose did that. The goal is to **isolate the causal mechanism** so the existing maintenance candidates can be evaluated against it.

## Scope Check

Question covers goal: YES. The question explicitly asks for primary cause + ranked secondary causes; the goal asks for one paragraph + ranking + maintenance evaluation. Tight match.

Specific-vs-pattern check: this inquiry is about the SPECIFIC instance (the Memory failure in the metaloop ladder run). The prior loop_diagnose covered the broader-pattern question (term-ambiguity-in-load-bearing-context recurrence). Stay narrow here — drill into THIS instance's causal mechanism. If the mechanism turns out to be project-wide (i.e., applies to terms in many inquiries, not just Memory), surface that as a finding without scope-creeping.
