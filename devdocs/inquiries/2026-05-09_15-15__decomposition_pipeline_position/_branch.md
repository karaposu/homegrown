# Branch: Decomposition pipeline position

## Question

Is Decomposition's current position (between Sensemaking and Innovation in the MVL+ pipeline E → S → **D** → I → C) the right architectural placement for it — or should it move to the front (decomposing the input query into independent piece-loops), move to after Critique (producing sub-paths for next-run MVL), be removed entirely from this loop (it belongs to a different kind of logic), or be kept where it is (corpus evidence supports current placement)?

## Goal

Produce a verdict on Decomposition's role and position in the cognitive-loop architecture, with concrete options the user can decide between. The verdict must:

1. Map the candidate positions (D-FIRST / D-CURRENT / D-AFTER-C / D-REMOVED / D-MULTIPLE) and any hybrid configurations that surface during exploration.
2. Distinguish between *operations* that look like decomposition but are actually different (e.g., "decomposing the input query into loops" vs. "decomposing the post-Sensemaking actionable space into pieces" — these may be different cognitive operations even if they share the name).
3. Reconcile with the just-completed audit (`devdocs/inquiries/2026-05-09_11-54__decomposition_value_audit/finding.md`), whose verdict said D produces modest formalization value and 0 NEGATIVE outputs at current position — but did not test the alternative positions.
4. Produce a recommendation (KEEP-CURRENT / RELOCATE-TO-X / REMOVE / SPLIT-INTO-MULTIPLE-OPERATIONS) with phase-fit / cost / blast-radius / corpus-grounding tags.

The user should be able to read the output and decide: leave the pipeline as E→S→D→I→C, or change the loop's architecture in a specific way.

## Scope Check

Question covers goal. The question names four candidate positions + a "framing-wrong" alternative; the goal extends to mapping hybrids and distinguishing operations that share the name "decomposition" but may be different cognitive operations.

**Specific-vs-pattern check:** The question is about the broader pattern (architectural placement of a discipline in the cognitive loop), not about specific past inquiries. The just-completed audit's 10-inquiry corpus is one piece of evidence, not the inquiry's scope.

**What this inquiry is NOT doing:**
- It is not redoing the audit (the audit answered "does D produce value at current position"); this inquiry asks "is current position the right one."
- It is not designing new disciplines (D's existence is given; only its position/presence is up for inquiry).
- The audit's recommended Assembly A (within-D-spec refinements) is independent of this inquiry's verdict — the refinements help D wherever D sits.

**Self-reference risk explicitly acknowledged:** This inquiry will run E→S→D→I→C, which means D operates on this very inquiry's actionable space. The verdict on D's position will be informed by how D performs in this run. Honest self-evaluation is required.

## Relationships

- BUILDS-ON: `devdocs/inquiries/2026-05-09_11-54__decomposition_value_audit/finding.md` (the prior audit deferred this exact question to a separate inquiry; the user is opening it now rather than waiting for audit-again)
- RELATED: `devdocs/inquiries/2026-05-08_19-53__discipline_corpus_tidying_application/finding.md` (most recent prior inquiry; subject of audit + provided per-discipline tidy framework)

## Source Input

```
Maybe decomposition shouldnt the after sensemaking? maybe decomposition should be done at the very beginnign in order to decompose the input query into pieces? so each piece can have their own loop?

Or maybe it shuld run after critique to produce subpaths of what can be run next with MVL ?

My point is this , we included decomposition to mvl+ but maybe that was wrong decision? and it belongs to somewhere else or different kind of logic?

Or this is wrong? and actually decomposition helped a lot of inquiries samples to be better?
```
