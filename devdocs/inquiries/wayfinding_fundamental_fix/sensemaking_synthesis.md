# Sensemaking Synthesis: The Wayfinding Fix

## SV6 — Two methods converge. Fix is clear.

### Root cause (settled, HIGH confidence)
Wayfinding's definition scopes it to state awareness. Its core question demands action evaluation. The "not planning" constraint prevented the required capability.

### The fix bundle (4 changes)

**Change 1 (command — primary):** Add goal-directed evaluation step between Read Layers and Produce Move.
```
1. State the goal (from input or _branch.md)
2. Enumerate 5-7 possible actions (2+ not on any plan)
3. Assess impact toward goal (HIGH/MEDIUM/LOW/NEGATIVE)
4. Produce move from highest-impact action (explain if overriding layers)
```

**Change 2 (command — reachability):** "Verify gates are REAL prerequisites, not plan orderings. Would X actually fail without Y?"

**Change 3 (spec — identity):** "Not planning" → "Not full project planning. But DOES evaluate actions against the stated goal."

**Change 4 (spec — present layer):** Add Goal row to present layer table.

### Properties of the fix
- Additive (doesn't remove/change existing components)
- Override explanation is the quality mechanism (bad ratings become visible)
- Addresses 4/4 symptoms (3.5 from instruction + 0.5 from reachability clarification)
- Real-world effectiveness: untested

### For innovation
Seed: how should the instruction be formulated for maximum effectiveness? Root cause and fix type are settled. Innovate on formulation variants only.

### Telemetry
Perspectives: 3 (2 new anchors). Ambiguity: 2/2 resolved. SV delta: moderate. Synthesis pass, not discovery.
