# Decomposition: Refactor a Sensemaking perspective into 2 to catch frame-exit?

## Step 1 — Perceive Coupling Topology

### Elements in the whole

From Sensemaking's stabilized model:

1. **Refactor candidate identification** — Definitional / Consistency is THE candidate; evidence = D/C bundles 3 cognitive jobs in spec text.
2. **Split design** — sub-perspective specifications: Internal Consistency (3 existing jobs preserved, scoped within-inquiry) + Frame-exit Completeness (operationalizes broad spec scope).
3. **Gating predicate** — conjunctive: (i) inherited multi-value terms AND (ii) organizing frames; analogous to Phase/Calibration-State conditional pattern.
4. **Non-hurt argument** — gating auto-skips on non-frame-bounded inquiries; cost zero on those.
5. **Refactor vs prior M7 comparison** — structurally equivalent net behavior; refactor preferable for honoring D/C's existing broad spec.
6. **Commitment status** — DEFERRED per loop_diagnose Step 5; revival trigger M4 audit ≥3 instances.
7. **Final recommendation** — defer with concrete revival trigger; refactor preferable but M7 is acceptable alternative.

### Pairwise coupling

| A | B | Coupling | Reason |
|---|---|---|---|
| Refactor candidate (#1) | Split design (#2) | **Strong** | Split is OF the candidate; #1 names what's being split, #2 specifies how |
| Split design (#2) | Gating predicate (#3) | **Strong** | Gating is part of the Frame-exit Completeness sub-perspective's spec text |
| Split design + gating (#2+#3) | Non-hurt argument (#4) | **Strong** | Argument's mechanism IS the gating predicate's auto-skip behavior |
| Split design (#2) | M7 comparison (#5) | **Moderate** | Comparison weighs split-design vs M7's add-design; reads the split details |
| Anything | Commitment status (#6) | **Moderate** | Commitment depends on overall design quality + Step 5 + N=1 evidence |
| Commitment + design (#6 + lower) | Recommendation (#7) | **Strong** | Recommendation IS commitment + ranked alternatives |

**Cluster identification:**
- **Hub cluster:** elements #1, #2, #3 fused — refactor candidate + split design + gating predicate together describe the SAME design object (the proposed refactor).
- **Spoke A:** non-hurt argument (#4) reads the design.
- **Spoke B:** M7 comparison (#5) reads the design.
- **Synthesis:** commitment + recommendation (#6 + #7) reads everything.

### Coupling map

```
                ┌──────────────────────────────────────────────┐
                │ P1: Refactor design (HUB)                    │
                │   - Candidate: Definitional / Consistency    │
                │   - Split: Internal Consistency +            │
                │              Frame-exit Completeness         │
                │   - Gating predicate (inherited multi-value  │
                │              terms + organizing frames)      │
                └────────────────┬─────────────────────────────┘
                                 │
              ┌──────────────────┼──────────────────┐
              │                  │                  │
              ▼                  ▼                  ▼
     ┌──────────────┐   ┌──────────────┐   ┌────────────────┐
     │ P2: Non-hurt │   │ P3: Refactor │   │ (synthesis)    │
     │   argument   │   │     vs M7    │   │                │
     │   (generic   │   │   comparison │   │                │
     │   capability)│   │              │   │                │
     └──────┬───────┘   └──────┬───────┘   │                │
            │                  │           │                │
            └──────────┬───────┘           │                │
                       │                   │                │
                       ▼                   │                │
              ┌────────────────────┐       │                │
              │ P4: Commitment +   │◀──────┘                │
              │     Recommendation │                        │
              │     (DEFERRED;     │                        │
              │     revival = M4   │                        │
              │     audit ≥3)      │                        │
              └────────────────────┘                        │
```

**Pieces consolidated:**
- **P1: Refactor design (hub)** — candidate + split + gating, fused.
- **P2: Non-hurt argument** — generic-capability check.
- **P3: Refactor vs M7 comparison** — alternatives weighing.
- **P4: Commitment + Recommendation** — DEFERRED with revival trigger; final verdict.

4 pieces is appropriate for a focused drill-down. Per Decomposition's stopping criterion: tractable.

---

## Step 2 — Detect Boundaries (Top-Down)

Cuts at low coupling:

- **B-1: Between P1 and P2.** Crossing traffic: P2 reads the gating predicate. One read. Low.
- **B-2: Between P1 and P3.** Crossing traffic: P3 reads the split design. One read. Low.
- **B-3: Between P2/P3 and P4.** Crossing traffic: P4 reads upstream. Low.

All boundaries clean.

---

## Step 3 — Validate Boundaries (Bottom-Up)

### Atoms (irreducible elements)

- A1: D/C as refactor candidate (single atom; named).
- A2: 3-jobs evidence (forward consistency, anchor-strength weighting, reverse self-consistency).
- A3: Internal Consistency sub-perspective spec text.
- A4: Frame-exit Completeness sub-perspective spec text.
- A5: Gating predicate (conjunctive: inherited multi-value AND organizing frame).
- A6: Non-hurt argument (auto-skip on non-frame-bounded).
- A7: M7 comparison (structurally equivalent; refactor preferable).
- A8: Commitment status (DEFERRED).
- A9: Revival trigger (M4 audit ≥3 instances).
- A10: Recommendation/verdict (refactor recommended; M7 acceptable; both deferred).

### Atom-to-piece grouping

| Atoms | Piece |
|---|---|
| A1, A2, A3, A4, A5 (refactor design) | P1 |
| A6 (non-hurt) | P2 |
| A7 (M7 comparison) | P3 |
| A8, A9, A10 (commitment + recommendation) | P4 |

### Boundary checks

- **A3 + A4 (sub-perspective spec text) inside P1?** Yes. They describe the SAME design object (the split). Splitting them across pieces would create high-bandwidth interface (e.g., "Internal Consistency depends on Frame-exit Completeness's gating to define its scope"). Keep together in P1.
- **A5 (gating predicate) inside P1 or P2?** P1 — the gating is part of A4's spec text. P2 (non-hurt argument) READS A5 to make its argument; doesn't own it.
- **A6 (non-hurt argument) and P1's gating predicate (A5)?** Loose coupling — P2 reads A5 once. Boundary clean.

### Top-down vs bottom-up

| Boundary | Top-down location | Bottom-up confirms? |
|---|---|---|
| P1 ↔ P2 | refactor design vs non-hurt argument | ✓ |
| P1 ↔ P3 | refactor design vs M7 comparison | ✓ |
| P2/P3 ↔ P4 | upstream vs synthesis | ✓ |

All confirmed. **Confidence: HIGH.**

---

## Step 4 — Express as Question Tree

### P1 — Refactor design (HUB)

**Question:** What is the refactor candidate, what does the split look like (sub-perspective spec text for both halves), and what is the gating predicate that triggers the new sub-perspective conditionally?

**Verification criteria:**
- [ ] Refactor candidate named: Definitional / Consistency.
- [ ] 3-jobs evidence cited: forward consistency, anchor-strength weighting, reverse self-consistency, all from `homegrown/sense-making/references/sensemaking.md` line 213.
- [ ] Internal Consistency sub-perspective spec text drafted (≤6 lines); preserves the 3 existing jobs; scopes explicitly to within-inquiry stabilized models.
- [ ] Frame-exit Completeness sub-perspective spec text drafted (≤8 lines); operationalizes broad spec scope; includes the conjunctive gating predicate.
- [ ] Gating predicate: conjunctive (i) inherited multi-value terms AND (ii) organizing frames; uses Phase/Calibration-State's existing conditional pattern.
- [ ] Both sub-perspectives use the spec's existing 2-level naming pattern ("Definitional / X").
- [ ] Examples included in spec text (Memory; suspected Reflect-channel) to ground the gating predicate.

### P2 — Non-hurt argument

**Question:** Does the proposed refactor's gating predicate auto-skip on non-frame-bounded inquiries (preserving generic Sensemaking capability), and what is the empirical-validation path?

**Verification criteria:**
- [ ] Description of "non-frame-bounded inquiry" with examples (code review; bug diagnosis; concrete strategic decision).
- [ ] Argument that the conjunctive gating predicate (inherited multi-value AND organizing frame) yields FALSE on non-frame-bounded inquiries → sub-perspective skips.
- [ ] Cost on non-frame-bounded inquiries: zero or near-zero.
- [ ] Risk acknowledgment: subjective gating could over-fire under loose interpretation; mitigated by spec-text examples + conjunctive (not disjunctive) gating.
- [ ] Empirical-validation path: post-revival-deployment, monitor next 5 Sensemaking outputs across inquiry types — does the sub-perspective fire spuriously?

### P3 — Refactor vs prior M7 comparison

**Question:** How does the refactor compare to the prior M7 candidate (add new "Frame-exit" perspective), and why is refactor preferable structurally — while acknowledging M7 remains a defensible alternative?

**Verification criteria:**
- [ ] Net behavior comparison: refactor and M7 both produce a frame-exit operation in Sensemaking; structurally equivalent.
- [ ] Conceptual difference: refactor honors D/C's existing broad spec; M7 asserts a missing capability.
- [ ] Naming-pattern comparison: refactor uses 2-level "Definitional / X" pattern (compatible with spec); M7 adds a new top-level perspective (also compatible).
- [ ] Recommendation: refactor preferred; M7 acceptable alternative if user prefers flat-list framing.
- [ ] If refactor is adopted, the prior M7 from `2026-05-09_21-15__loop_diagnose__memory_ambiguity_in_metaloop_ladder/finding.md` is superseded (with the same revival trigger).

### P4 — Commitment + Recommendation

**Question:** Given the design analysis (P1) + non-hurt argument (P2) + M7 comparison (P3) + N=1 evidence + `homegrown/protocols/loop_diagnose.md` Step 5 — what is the commitment status, the revival trigger, and the final recommendation?

**Verification criteria:**
- [ ] Commitment status: DEFERRED.
- [ ] Justification: Step 5 spirit — N=1 doesn't ship spec changes regardless of design quality.
- [ ] Revival trigger: M4 audit ≥3 instances of frame-scope-capture-like patterns across distinct inquiries.
- [ ] Recommendation: at revival, prefer refactor over M7; M7 deprecated with reference to this finding.
- [ ] Both user MUST conditions explicitly verified: (a) gap is real per D/C broad spec + Memory failure evidence; (b) generic capability not hurt under conjunctive gating per non-hurt argument.
- [ ] User can take this finding back to the prior loop_diagnose finding and update its DEFERRED maintenance candidates list (prior M7 → updated to "refactor D/C with M7 as alternative").

---

## Step 5 — Map Interfaces

| From | To | What flows | Direction | Type |
|---|---|---|---|---|
| P1 | P2 | Gating predicate (A5) | one-way | Read |
| P1 | P3 | Split design (A3 + A4 + A5) | one-way | Read |
| P1 | P4 | Full design summary | one-way | Read |
| P2 | P4 | Non-hurt argument result (auto-skip claim) | one-way | Read |
| P3 | P4 | M7 comparison verdict (refactor preferable; M7 acceptable) | one-way | Read |

All flows one-way Reads. No bidirectional, no shared state, no circular.

### Hidden coupling check

- **Q: Does P3 (M7 comparison) hide-couple with P2 (non-hurt argument)?** P3 might say "M7 also has the same non-hurt property because M7's frame-exit perspective could be similarly gated." Make explicit: P3 should note that the non-hurt argument (P2) applies equally to M7 if M7's perspective is conditionally gated. So the comparison is purely about FRAMING (refactor vs add), not about non-hurt difference. Update P3's verification criterion 5 to make this explicit.

- **Q: Does P4 hide-couple with the prior loop_diagnose finding?** Yes — P4's recommendation references updating the prior finding's deferred-candidates list. This is an explicit cross-finding interface.

No remaining hidden coupling.

---

## Step 6 — Order by Dependency

```
Step 1: P1 (HUB — refactor design)
Step 2: P2 (non-hurt) + P3 (M7 comparison) — parallel, both read P1
Step 3: P4 (commitment + recommendation) — synthesizes P1, P2, P3
```

### Reasoning

- **P1 first:** the hub. Every other piece reads from it.
- **P2 + P3 in parallel:** independent reads of P1.
- **P4 last:** synthesis.

### Critical path

P1 → P4 via P2 + P3.

### No circular dependencies

Verified.

---

## Step 7 — Self-Evaluate

### Minimum 3-dimension evaluation

| Dimension | Check | Verdict | Notes |
|---|---|---|---|
| **Independence** | Each piece's question answerable without sibling pieces? | **PASS** | P2/P3 each read P1 only; P4 reads P1+P2+P3. No piece needs another's interior content. |
| **Completeness** | Pieces cover the whole? | **PASS** | All 7 elements (#1-#7 from Step 1) are covered by 4 pieces. No gap. |
| **Reassembly** | Pieces + interfaces = whole? | **PASS** | P1 + P2 + P3 + P4 → complete recommendation answering "should we refactor? if so, how? does it hurt generic? defer or ship?" |

### Determination-mechanism check

- **Determination at runtime:** "should the deferred refactor be revived to ACTIONABLE?" — depends on M4 audit results.
- **Piece addressing how the check is performed:** P4 explicitly owns this. P4's verification criterion 3 specifies the revival trigger as "M4 audit ≥3 instances of frame-scope-capture-like patterns." Pass.

- **Determination at runtime:** "does the gating predicate fire on a given Sensemaking run?" — depends on whether the run has inherited multi-value terms + organizing frames.
- **Piece addressing how the check is performed:** P1 explicitly owns this (gating predicate spec). P1's verification criterion 5 specifies the conjunctive gating with examples. Pass.

### Full 7-dimension evaluation

| Dimension | Verdict | Notes |
|---|---|---|
| Independence | PASS | (above) |
| Completeness | PASS | (above) |
| Reassembly | PASS | (above) |
| Tractability | PASS | P1 hub is the largest (drafts 2 sub-perspective spec texts); P2-P3-P4 are single-paragraph or short-section pieces. All small. |
| Interface clarity | PASS | All flows tabulated; one-way Reads; hidden coupling surfaced (P3 + P2 relationship made explicit). |
| Balance | **PASS** | More balanced than prior decompositions. P1 hub is heavier than spokes; intentional and matches the deliverable shape. |
| Confidence | PASS | Top-down ↔ bottom-up agreed. |

### Failure-mode self-check

- **Premature decomposition?** No — Sensemaking provided clear chain structure before decomposition.
- **Wrong boundaries?** No — boundaries cut at low-coupling regions; design elements stay together (high internal coupling within P1).
- **Hidden coupling?** Surfaced (P3 ↔ P2 relationship); made explicit in P3 verification criterion.
- **Missing pieces?** No — determination-mechanism check passed; all deliverable elements covered.
- **Over-decomposition?** No — 4 pieces for a focused drill-down. None trivial.
- **Ignoring dependencies?** No — Step 6 explicit.
- **Imbalanced?** No — proportional.

---

## Final Deliverable

### Coupling Map

One hub (P1: refactor design with candidate + split + gating fused), two spokes (P2: non-hurt argument; P3: M7 comparison), one synthesis (P4: commitment + recommendation).

### Question Tree

- **P1 — Refactor design** (hub): D/C as candidate; split into Internal Consistency + Frame-exit Completeness; gating predicate.
- **P2 — Non-hurt argument** (spoke): conjunctive gating auto-skips on non-frame-bounded inquiries.
- **P3 — Refactor vs M7 comparison** (spoke): structurally equivalent; refactor preferable for honoring D/C's broad spec.
- **P4 — Commitment + Recommendation** (synthesis): DEFERRED per Step 5; revival = M4 audit ≥3 instances; refactor recommended over M7 at revival.

### Interface Map

| From | To | Flow | Type |
|---|---|---|---|
| P1 | P2 | Gating predicate | Read |
| P1 | P3 | Split design | Read |
| P1 | P4 | Full design | Read |
| P2 | P4 | Non-hurt argument result | Read |
| P3 | P4 | M7 comparison verdict | Read |

### Dependency Order

```
Step 1: P1 (hub)
Step 2: P2 + P3 (parallel, read P1)
Step 3: P4 (synthesis, reads P1+P2+P3)
```

### Self-Evaluation

| Dimension | Verdict |
|---|---|
| Independence | PASS |
| Completeness | PASS |
| Reassembly | PASS |
| Tractability | PASS |
| Interface clarity | PASS |
| Balance | PASS |
| Confidence | PASS |
| Determination-mechanism check | PASS |

**8/8 PASS.** Decomposition committed; proceed to Innovation.

---

## Notes for next discipline

**Innovation should:**
1. Commit P1 — draft actual sub-perspective spec text for Internal Consistency + Frame-exit Completeness; include the gating predicate with conjunctive (AND) structure and concrete examples.
2. Commit P2 — non-hurt argument with examples of non-frame-bounded inquiries.
3. Commit P3 — M7 comparison verdict with explicit equivalence note.
4. Commit P4 — DEFERRED commitment with M4-audit revival trigger; final recommendation.
5. Apply minimum mechanism coverage on the new spec text (1 generator + 1 framer to confirm shape).

**Critique should:**
1. Test whether the drafted sub-perspective spec text is substantive (not renaming-without-substance).
2. Test gating predicate tightness — would it fire on this very inquiry? (Yes — this inquiry has inherited multi-value terms ["Sensemaking perspectives"] and an organizing frame [the 8-perspective structure]; the test should fire and validate that the design is self-consistent.)
3. Test self-reference resistance.
4. Verify Step 5 deferral honored.
