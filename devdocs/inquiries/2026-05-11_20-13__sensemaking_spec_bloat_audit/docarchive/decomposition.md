# Decomposition: Sensemaking Spec Bloat Audit

## User Input

Source: `/Users/ns/Desktop/projects/native/devdocs/inquiries/2026-05-11_20-13__sensemaking_spec_bloat_audit/_branch.md`

Whole to decompose: produce Innovation's deliverable per Sensemaking's SV6 — 6 surgical edit drafts to `homegrown/sense-making/references/sensemaking.md` (B1 generalize-example; B2 delete; B3-B6 reword) plus Research Frontier observations.

---

## Step 1 — Perceive Coupling Topology

### Elements

1. **E1.** B1 generalize-example: draft generic labels for Frame-exit Completeness positive example (line 261), preserving demonstration structure (ladder inquiry inheriting multi-value term; TYPE-axis surfacing referent types; Role Assessment finds load-bearing referent; verdict = re-locate).
2. **E2.** B2 delete-entirely: identify exact text of Accommodation trigger Frontier-flag parenthetical (line 378) to delete.
3. **E3.** B3 reword: enumerate substitutions for Load-bearing concept test wording (lines 334-338) — "the loop" → "the analysis"; "the project's vocabulary" → "the inquiry's vocabulary"; "loop-coined" → "newly-coined"; etc.
4. **E4.** B4 reword: single substitution for "trigger-classifier rules" (line 338) → "rules whose firing depends on a runtime determination."
5. **E5.** B5 reword: single substitution for "from one inquiry, instances from one corpus chain" (line 344) → "from a small set of observations or one chain of related cases."
6. **E6.** B6 reword: enumerate "project-wide" occurrences in Frame-exit Completeness (multiple lines) + substitution → "scope-wide" (or "outside the inquiry's frame" where clearer).
7. **E7.** Research Frontier: B2 content destination (where the Frontier-flag's content optionally lives: originating inquiry's finding, `homegrown/protocols/loop_diagnose.md`, or unpreserved).
8. **E8.** Research Frontier: cross-discipline-spec audit (other discipline specs may have analogous bloat).

### Coupling

- **E1** is structurally heavier than E2-E6 — drafting a new generic example requires design work (choosing generic labels that demonstrate the procedure without project context). Distinct.
- **E2-E6** are all mechanical text-substitution drafts — each enumerates an exact reword pair or deletion target. Tightly coupled by fix-shape (all are simple substitutions).
- **E2** specifically is a deletion (no replacement text), simpler than the others — but still mechanical text-target identification.
- **E7-E8** are observations only — flagged as out-of-scope for the cleanup deliverable; loosely coupled with each other (independent topics).

### Clusters

- **Cluster A — Generalize-example (heavy)** {E1}: requires generic-label design.
- **Cluster B — Substitution drafts (mechanical)** {E2, E3, E4, E5, E6}: each is a text-target identification + (for B3-B6) a reword pair.
- **Cluster C — Research Frontier observations** {E7, E8}.

---

## Step 2 — Detect Boundaries

Three pieces:

- **P1 — B1 generalize-example draft** {E1}: heavy; design generic example labels.
- **P2 — B2-B6 substitution drafts** {E2, E3, E4, E5, E6}: mechanical; enumerate text targets and reword pairs.
- **P3 — Research Frontier observations** {E7, E8}: out-of-scope observations.

### Boundary qualities

- P1 ↔ P2: P1 produces example text; P2 produces substitution lists. Mutually independent — neither reads the other's output.
- P1 ↔ P3: P3 doesn't depend on P1.
- P2 ↔ P3: P3 references the cleanup as context but doesn't depend on P2's substitution drafts.

All boundaries one-way Reads (or no flow at all between mutually independent pieces).

---

## Step 3 — Validate Boundaries (Bottom-Up)

### Atoms

| Atom | Description |
|---|---|
| A1 | B1 generic example labels (replacement subjects for /MVL+, metaloop-ladder, session-architecture finding) |
| A2 | B1 demonstration structure preserved (ladder inquiry; TYPE-axis surfacing; Role Assessment; re-locate verdict) |
| A3 | B1 exact text for the new example sentence(s) |
| A4 | B2 exact text-target to delete (the Frontier-flag parenthetical from line 378) |
| A5 | B3 substitution pair 1 ("the loop's verdict" → "the analysis's verdict") |
| A6 | B3 substitution pair 2 ("the loop adopted" → "the analysis adopted") |
| A7 | B3 substitution pair 3 ("the project's actual vocabulary" → "the inquiry's vocabulary") |
| A8 | B3 substitution pair 4 ("loop-coined neologism" → "newly-coined term" or similar) |
| A9 | B3 substitution pair 5 ("the loop coined" → "the analysis coined") |
| A10 | B4 substitution ("trigger-classifier rules" → "rules whose firing depends on a runtime determination") |
| A11 | B5 substitution ("from one inquiry, instances from one corpus chain" → "from a small set of observations or one chain of related cases") |
| A12 | B6 occurrence enumeration ("project-wide" in lines 247, 253, etc.) |
| A13 | B6 substitution ("project-wide" → "scope-wide" or "outside the inquiry's frame") |
| A14 | Research Frontier 1: B2 content destination |
| A15 | Research Frontier 2: cross-discipline-spec audit |

### Atom grouping vs Step-2 boundaries

| Atom | Assigned to | Correct? |
|---|---|---|
| A1, A2, A3 | P1 | ✓ |
| A4, A5, A6, A7, A8, A9, A10, A11, A12, A13 | P2 | ✓ |
| A14, A15 | P3 | ✓ |

All atoms group cleanly. **Confidence: HIGH.**

---

## Step 4 — Express as Question Tree

### P1 — B1 generalize-example draft

**Question:** What generic example for the Frame-exit Completeness perspective preserves the demonstration structure (ladder inquiry inheriting a multi-value term; TYPE-axis surfacing multiple referent types; Role Assessment finds load-bearing referent; verdict = re-locate) without project-specific labels?

**Verification criteria:**
- [ ] Generic subject chosen (replaces "metaloop-ladder inquiry") that's plausible to a generic practitioner
- [ ] Generic multi-value term chosen (replaces "Memory") with 3+ identifiable referent types
- [ ] Generic prior-finding source chosen (replaces "session-architecture finding")
- [ ] Generic referent types enumerated (3 types, each plausible at the TYPE-axis)
- [ ] Generic load-bearing referent identified
- [ ] Verdict (re-locate not exclude) preserved
- [ ] Total length comparable to original (~6 lines)
- [ ] Reads naturally — doesn't feel forced or artificial

### P2 — B2-B6 substitution drafts

**Question:** What are the exact text-targets and substitution pairs for the 5 mechanical edits (B2 deletion + B3 multi-substitution + B4 single substitution + B5 single substitution + B6 multi-occurrence substitution)?

**Verification criteria:**
- [ ] B2 deletion target identified exactly (the parenthetical to remove)
- [ ] B3 substitution pairs enumerated (≥5 substitutions across lines 334-338)
- [ ] B4 substitution pair stated
- [ ] B5 substitution pair stated
- [ ] B6 occurrences of "project-wide" enumerated (line numbers; in-context phrases)
- [ ] B6 substitution pair stated (with context-sensitive variant if needed)
- [ ] Each substitution preserves structural meaning (verified by Critique)
- [ ] No substitution introduces new ambiguity or contradicts existing spec content

### P3 — Research Frontier observations

**Question:** What out-of-scope observations need capture as Research Frontiers?

**Verification criteria:**
- [ ] Research Frontier 1: B2 content destination (where the Frontier-flag's content optionally lives) — flagged as separate user decision
- [ ] Research Frontier 2: cross-discipline-spec audit (other 4 discipline specs may have analogous bloat) — flagged for separate inquiry
- [ ] Both marked out-of-scope for THIS cleanup's deliverable

---

## Step 5 — Map Interfaces

### Internal interfaces

| # | Source | Target | What flows | Direction |
|---|---|---|---|---|
| (none) | — | — | P1, P2, P3 are mutually independent (no flows between them) | — |

The three pieces have NO internal interfaces — they don't share content. P1 produces example text; P2 produces substitution lists; P3 produces observations. None reads the others' output.

### External interfaces

| # | Source | Target | What flows | Direction |
|---|---|---|---|---|
| I1 | (External — Sensemaking SV6) | P1, P2, P3 | Stabilized fix shapes per instance; shipping strategy; out-of-scope decisions | One-way Read |
| I2 | (External — Exploration) | P1, P2 | Bloat inventory with line numbers + per-instance reasoning | One-way Read |
| I3 | (External — `homegrown/sense-making/references/sensemaking.md`) | P1, P2 | Current spec text (the artifact being edited) | One-way Read |

All one-way Reads. Zero bidirectional flows.

---

## Step 6 — Order by Dependency

```
P1 (B1 generalize-example; HEAVY)        — independent
P2 (B2-B6 substitution drafts; BULK)     — independent
P3 (Research Frontier observations)      — independent
```

All three pieces are mutually independent of each other (depend only on external sources: Sensemaking SV6, Exploration, the current spec).

**Order:** all three can run in parallel. Innovation drafts P1, P2, P3 concurrently with no ordering constraints between them.

---

## Step 7 — Self-Evaluate

### Minimum (3 dimensions)

| # | Dimension | Result |
|---|---|---|
| 1 | Independence | **PASS.** P1, P2, P3 mutually independent. Each workable alone given external inputs (Sensemaking SV6 + Exploration + current spec). |
| 2 | Completeness | **PASS.** P1+P2+P3 covers Sensemaking SV6's full handoff: B1 generalize-example (P1); B2-B6 substitutions (P2); Research Frontier observations (P3). |
| 3 | Reassembly | **PASS.** Pieces compose into the complete Innovation deliverable: the 6 surgical edit drafts + Research Frontier captures. CONCLUDE bundles these into finding.md. |

### Determination-mechanism piece check

Load-bearing concept: "the universal-discipline test" — its runtime determination is "would a generic practitioner using this discipline on an unrelated project find this text meaningful?" The determination is specified in Exploration (Section 2 — "Discrimination criterion") and applied per-instance in Sensemaking's ambiguities. P1 and P2 each implement the test by producing texts that pass it. **PASS.**

### Full (7 dimensions)

| # | Dimension | Result |
|---|---|---|
| 4 | Tractability | **PASS.** P1 ~150-200 words (one generic example replacing the project-specific one). P2 ~400 words (5 substitution lists + 1 deletion target + occurrence enumeration). P3 ~100 words (2 observations). All small. |
| 5 | Interface clarity | **PASS.** Zero internal interfaces; 3 external one-way Reads. No hidden coupling — P1 doesn't need P2's substitutions to draft the example; P3 doesn't need either. |
| 6 | Balance | **PASS** with caveat. P2 is largest (~400 words) because it bundles 5 mechanical edits. P1 (~150-200) and P3 (~100) are smaller. Roughly proportional to scope of each piece's work (P2 bundles 5 fixes; P1 is one structural draft; P3 is observation-only). Balance acceptable. |
| 7 | Confidence | **HIGH.** Top-down (Step 2's clusters) and bottom-up (Step 3's atom grouping) agree on all 3 pieces. |

**7/7 PASS.**

---

## Final Deliverable Summary

### Coupling Map

- **Cluster A — Generalize-example (heavy)** {E1}: 1 element, structurally heavier work (generic-label design).
- **Cluster B — Substitution drafts (mechanical)** {E2, E3, E4, E5, E6}: 5 elements, all mechanical text-target + reword identifications.
- **Cluster C — Research Frontier observations** {E7, E8}: 2 elements, out-of-scope observation flags.

### Question Tree

| # | Question | Tractable? |
|---|---|---|
| P1 | What generic example preserves the Frame-exit demonstration structure without project-specific labels? | Yes |
| P2 | What are the exact text-targets and substitution pairs for the 5 mechanical edits (B2 deletion + B3/B4/B5/B6 rewords)? | Yes |
| P3 | What out-of-scope observations need capture as Research Frontiers? | Yes |

### Interface Map

- Zero internal interfaces (P1, P2, P3 mutually independent)
- 3 external one-way Reads (Sensemaking SV6; Exploration; current `sensemaking.md`)
- Zero bidirectional flows

### Dependency Order

All three pieces run in parallel (mutually independent).

### Self-Evaluation: 7/7 PASS

**Decomposition ready for Innovation.**
