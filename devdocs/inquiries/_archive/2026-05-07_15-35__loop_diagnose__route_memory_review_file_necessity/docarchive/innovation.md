# Innovation: Loop Diagnose - Route Memory Review File Necessity

## User Input

LOOP_DIAGNOSE on the correction chain. Sensemaking produced four primary hypotheses (A, B, C, D) at HIGH, one framing (D-prime) at MEDIUM-HIGH, two propagation hypotheses (Decomposition, Innovation mechanism) at MEDIUM, and two cross-chain patterns (P1, P2) at Monitoring tier. Decomposition produced a 10-piece question tree mapped to LOOP_DIAGNOSE Step 4 schema plus cross-chain extension. Innovation now generates candidate maintenance interventions tied to primary hypotheses, narrow enough to avoid fundamentals rewrites, with explicit evaluation gates, composing with previous LOOP_DIAGNOSE M1-M9 candidates rather than replacing them.

## Seed

Seed type: gap + collision + recurrence.

The gap: each of the four primary hypotheses identifies a specific process-level absence in the prior loop (corpus omission, principle adopted as inverse axiom, candidate space on one axis, dimension list missing cultural-fit).

The collision: the new candidates must compose with the previous LOOP_DIAGNOSE M1-M9 set (specifically extending M1 anchor-interrogation to cover Foundational Principles in addition to Constraints, and adding a sister candidate to M3's duplicate-derivable-state dimension).

The recurrence: cross-chain patterns P1 (Sensemaking Phase 1 axiom items) and P2 (Critique missing project-specific dimension) are at 2 of 3+ chain threshold. Innovation should not generate cross-cutting protocol-level rules from the recurrence alone but should make the per-stage candidates strong enough to bridge into the cross-cutting rule once the third chain confirms.

Valuation: highest expected benefit per byte of patch sits at upstream surfaces (Exploration corpus + Sensemaking principle); detective surfaces (Critique dimension) provide redundant coverage; runner-level surfaces (D-prime framing) accumulate cross-chain evidence rather than generating their own per-chain candidate.

Motivation: produce 5-9 candidate interventions across the mechanism space; converge on 4-6 ACTIONABLE candidates plus 2-4 strengthened previous + content cleanup + monitoring extensions.

## Mechanism Outputs

### 1. Lens Shifting

Generic variation:

- View each maintenance candidate as a per-discipline patch (one rule per spec file).

Focused variation:

- View the maintenance lever as belonging to one of four tiers: corpus-input (Exploration's intake of cultural-norm corpus), principle-extraction (Sensemaking's Phase 1), candidate-space (Innovation's mechanism execution boundary), and dimension-articulation (Critique's Phase 0). Each tier has its own corrective surface; each can be patched independently with one paragraph.

Contrarian variation:

- View any source edit from this single chain as overreach because the previous LOOP_DIAGNOSE already proposed M1 (anchor interrogation) targeting the same family. Do not duplicate; just extend M1's recognition cues and propose nothing new.

Surviving output:

The lens-shift produces three frames: per-tier preventive (per-discipline patches), composition-with-previous (extend M1, M3 rather than duplicate), and recurrence-monitoring (let cross-chain patterns accumulate). All three apply at different evidence weights: per-tier patches for HIGH-confidence per-chain hypotheses, composition for clear extensions of existing candidates, monitoring for sub-threshold cross-chain observations.

### 2. Combination

Generic variation:

- Combine all primary-hypothesis candidates into one cross-cutting rule.

Focused variation:

- Combine N1 (Exploration corpus scan) with the upstream side of B (Sensemaking principle source verification): Exploration's Coarse Scan should produce a "cultural-norm corpus loaded" check that Sensemaking's Phase 1 inherits as a precondition before adopting Foundational Principles. This is two patches that compose mechanically.

Contrarian variation:

- Combine the new candidates with cross-chain pattern observations (P1, P2) to argue for a single cross-discipline rule covering all anchor-or-dimension shortcomings. This is the M6 cross-cutting "name-and-test" pattern from previous LOOP_DIAGNOSE.

Surviving output:

Focused combination is strong: N1 (corpus-scan rule for Exploration) and M1+N2 (anchor-interrogation extended to Foundational Principles) compose mechanically — the corpus is the input, the principle is the output, the interrogation is the verification. The contrarian cross-cutting M6 promotion is rejected on chain-count grounds (2 of 3+ threshold not met).

### 3. Inversion

Generic variation:

- Instead of "patch each discipline to prevent recurrence," patch the runner to log when these patterns occur, building observability before adding rules.

Focused variation:

- Instead of "Sensemaking should interrogate its anchors," ask "what would have to be visible for the cultural-fit failure to be impossible to miss?" If the Sensemaking output structure included a "Foundational Principle Source Verification" subsection citing where each principle came from, the cultural-fit failure would be observable at Sensemaking-output time, before downstream stages inherit it.

Contrarian variation:

- Instead of fixing the loop, fix the input. A pre-loop step that loads project-cultural-norm files into context before any inquiry begins. This shifts responsibility upstream of MVL+ entirely.

Surviving output:

Focused inversion adds a strong candidate: a Sensemaking output requirement that surfaces the source of each Foundational Principle. This is detection-by-output rather than detection-by-rule. The contrarian variation is partly useful but routes to runner-level pre-loop step, where the evidence is one chain.

### 4. Constraint Manipulation

Generic variation:

- Add constraint: every maintenance candidate must have an observable evaluation gate.

Focused variation:

- Add constraints: (a) each new candidate must be one paragraph in a discipline reference file or runner spec; (b) the evaluation gate must fire within the next 3 MVL+ runs that are relevant to the candidate's surface; (c) the candidate must not modify SIC/MVL+ pipeline structure; (d) cross-chain patterns must not promote without 3+ chain count.

Contrarian variation:

- Remove the "candidate must be a source edit" constraint. Allow candidates that are documentation, training notes, or process-level reminders.

Surviving output:

The focused constraint set is the right shape: one-paragraph patches, near-term gates, no pipeline rewrites, no protocol promotion from sub-threshold patterns. The contrarian relaxation is partly useful for borderline candidates (e.g., a documentation note about the cultural-norm corpus is a useful supplement to N1).

### 5. Absence Recognition

Generic variation:

- Missing object: a "cultural-norm corpus" reference list that codebase-context inquiries should always scan.

Focused variation:

- Missing object: a small, named list of files (`artifact_materialization.md`, `outcome_review.md`, `alignment_control.md`, plus future additions) that constitutes Homegrown's cultural-norm corpus. Exploration's corpus-scan rule references this list by name rather than asking the runner to identify cultural-norm files ad hoc.

Contrarian variation:

- Missing object: a "kill the prior finding" patch. The prior `finding.md` still recommends adaptive overlay; if a runner later resumes the prior in isolation, it might still execute that recommendation. The corrected `finding.md` already has `corrects:` frontmatter, but the prior is not annotated as superseded — same content-cleanup as previous LOOP_DIAGNOSE M7.

Surviving output:

The focused absence is a strong supplement to N1: rather than asking Exploration to identify cultural-norm files ad hoc, name them in a small list under `homegrown/explore/references/explore.md`. This makes recognition mechanical. The contrarian content-cleanup mirrors the previous LOOP_DIAGNOSE M7 pattern; same shape, applied to this chain's prior finding.

### 6. Domain Transfer

Generic variation:

- Transfer from code review: pre-commit checklists for specific failure patterns.

Focused variation:

- Transfer from technical writing / project documentation: every project has a small "voice and conventions" reference that contributors load before producing substantive content. Homegrown's equivalent would be a "cultural-norms manifest" — the small list mentioned in Mechanism 5 — that codebase-context inquiries always reference.

Contrarian variation:

- Transfer from incident postmortems: blameless-postmortem template captures what happened, what was assumed, what was missed. This is structurally equivalent to LOOP_DIAGNOSE itself; meta-recursive.

Surviving output:

Focused domain transfer reinforces the cultural-norms manifest idea (Mechanism 5 output). The contrarian meta-recursion is a confirmation that LOOP_DIAGNOSE is the right protocol; not a new candidate.

### 7. Extrapolation

Generic variation:

- As more correction chains accumulate, propose protocol-level changes only when patterns recur across 3+ chains.

Focused variation:

- The cross-chain patterns P1 (Sensemaking Phase 1 axiom items) and P2 (Critique missing project-specific dimension) are at 2 of 3+ chain threshold. The third LOOP_DIAGNOSE run is the gate. Per LOOP_DIAGNOSE Step 6, protocol-level promotion requires "5-10 stable diagnostic findings"; the 3+ chain threshold for cross-chain patterns is the protocol's intermediate gate. New candidates should be sized so that pattern-level promotion (M6) is not required for them to deliver value individually.

Contrarian variation:

- If LOOP_DIAGNOSE accumulates 3+ correction chains without producing reproducible patterns, the protocol's diagnostic method itself should be revisited (per LOOP_DIAGNOSE Step 6's "stable internal method" gate).

Surviving output:

The accumulation discipline reinforces sizing each candidate to deliver per-chain value while contributing cross-chain evidence. Cross-chain pattern observation is its own evidence track; protocol-level promotion remains gated.

## Candidate Designs

### Candidate N1 — Cultural-Norm Corpus Scan Rule In Exploration

Shape:

Add a one-paragraph rule to `homegrown/explore/references/explore.md` (Coarse Scan section, possibility/artifact mode for codebase-context inquiries): when scanning a codebase whose conventions or cultural norms are load-bearing for the inquiry, the Coarse Scan list must include the project's cultural-norm corpus before going deep on inquiry-specific files. For Homegrown specifically, this corpus is `homegrown/protocols/artifact_materialization.md`, `homegrown/protocols/outcome_review.md`, `homegrown/contracts/alignment_control.md`. Omitting these in a codebase-context Exploration is a Premature Depth signal.

Strength:

- Targets the most upstream cause directly.
- Names the corpus explicitly so recognition is mechanical, not judgment-dependent.
- Composes with the previous LOOP_DIAGNOSE M2 (probe-before-stabilization rule); N1 is breadth-before-depth, M2 is probe-after-listing.

Weakness:

- The named corpus list will need maintenance as the project's cultural-norm files change.
- "Codebase-context inquiry" is a judgment-dependent category; not every Exploration is a codebase-context Exploration.

Test:

- Novelty: medium. Builds on Premature Depth failure mode in `explore.md`.
- Scrutiny survival: high. Diagnosis evidence supports it directly.
- Fertility: high. Generalizes to other projects with documented cultural norms.
- Actionability: high. One paragraph plus a short list of file paths.
- Mechanism independence: high. Multiple mechanisms (Combination, Absence Recognition, Domain Transfer) converge.

Verdict: candidate.

### Candidate N2 (Strengthens Previous M1) — Sensemaking Phase 1 Anchor-Interrogation Extended To Foundational Principles

Shape:

Strengthen `homegrown/sense-making/references/sensemaking.md` Phase 1 anchor-interrogation rule (the previous LOOP_DIAGNOSE's M1) to cover BOTH Phase 1 / Constraints AND Phase 1 / Foundational Principles. The unified rule: for each Phase 1 / Constraints item phrased as a fixed property of the domain, AND for each Phase 1 / Foundational Principles item phrased as a project axiom, generate at least one Phase 3 ambiguity-collapse pair that tests whether the property/principle is the project's actual property/principle or an external default. Failing to produce such a pair is a Premature Stabilization signal at the principle layer.

Strength:

- Composes with previous M1 rather than replacing.
- Catches both layer instances (constraint + foundational principle).
- Cross-chain pattern P1 strengthens this candidate's recurrence evidence.

Weakness:

- Same judgment-dependency as M1 (recognizing "domain-property" or "project-axiom" phrasing).
- Adds slight cognitive load to Sensemaking Phase 3.

Test:

- Novelty: medium.
- Scrutiny survival: high.
- Fertility: high. Two layers are now covered with one rule.
- Actionability: high.
- Mechanism independence: high.

Verdict: candidate (extends M1).

### Candidate N3 — Innovation Candidate-Axis Decoupling Rule

Shape:

Add a one-paragraph rule to `homegrown/innovate/references/innovate.md` (Candidate Set / Assembly Check section): when generating candidates for a problem that combines an operation with output storage, generate candidates along TWO axes — operation-trigger control (when the operation runs) AND storage-policy control (what gets saved). Treat these as orthogonal degrees of freedom. A candidate set that varies only one axis is incomplete; the Assembly Check should explicitly test for the missing axis.

Strength:

- Targets the specific gap observed in this chain (one-axis candidate space).
- Generalizes to any operation+output design problem.
- Composable with the existing 7-mechanism set.

Weakness:

- "Operation" and "output storage" are somewhat abstract; specific recognition cues may need refinement.
- May produce candidate-set bloat in problems where only one axis matters.

Test:

- Novelty: medium-high.
- Scrutiny survival: high.
- Fertility: high.
- Actionability: medium-high (judgment-dependent in identifying when both axes apply).
- Mechanism independence: medium-high.

Verdict: candidate.

### Candidate N4 — Critique Explicit-Culture-Fit Dimension

Shape:

Add a one-paragraph rule to `homegrown/td-critique/references/td-critique.md` (Dimensions With Weights section): when evaluating candidates that produce, save, or omit project artifacts, include an "explicit-culture-fit" or "project-norm-fit" dimension that asks whether the candidate matches the project's documented artifact culture. For Homegrown specifically, the project's artifact culture is documented in the cultural-norm corpus (see `explore.md` corpus-scan rule). When generic anti-bloat heuristics conflict with explicit project culture, the project-norm-fit dimension takes precedence.

Strength:

- Targets the specific gap observed in this chain (missing cultural-fit axis).
- Composes with previous M3 (duplicate-derivable-state); M3 and N4 are sister dimensions, both targeting Critique's missing project-specific axes.
- Cross-chain pattern P2 strengthens this candidate's recurrence evidence.

Weakness:

- "Project's documented artifact culture" is judgment-dependent in projects with no such documentation.
- Adds one dimension to Critique's dimension list.

Test:

- Novelty: medium-high.
- Scrutiny survival: high.
- Fertility: high.
- Actionability: high.
- Mechanism independence: high.

Verdict: candidate.

### Candidate N5 — Sensemaking Foundational-Principle Source Verification (Output-Level)

Shape:

Add a one-paragraph rule to `homegrown/sense-making/references/sensemaking.md` (Phase 1 / Foundational Principles output requirement): each Foundational Principle item should include a brief source citation — where this principle comes from (project-specific corpus path, generic methodology, user input). Principles without an identifiable source are flagged at output time, not at runtime.

Strength:

- Detection-by-output rather than detection-by-rule.
- Cheaper than running Phase 3 ambiguity-collapse on every principle.
- Complements N2 (strengthening M1): N5 catches missed-citation-at-output, N2 catches missing-Phase-3-test.

Weakness:

- Sensemaking output gets slightly longer.
- "Source citation" is judgment-dependent (what counts as identifiable source?).
- Two patches at the same Sensemaking surface (N2 + N5) may overlap.

Test:

- Novelty: medium.
- Scrutiny survival: medium-high.
- Fertility: medium-high.
- Actionability: medium.
- Mechanism independence: medium.

Verdict: REFINE → candidate-with-overlap-flag. Decide whether N5 strengthens or duplicates N2 in Critique phase.

### Candidate N6 — Cultural-Norms Manifest File

Shape:

Create a small, durable Markdown file at `homegrown/contracts/cultural_norms.md` (or similar path) that names the project's cultural norms with citations to the canonical reference files. Exploration's corpus-scan rule (N1) references this manifest rather than naming files inline. Future additions to the cultural-norm corpus update the manifest, not every spec file that references it.

Strength:

- Single point of truth for cultural-norm corpus.
- Reduces maintenance burden on per-spec corpus lists.
- Creates an explicit artifact for the cultural-fit dimension to evaluate against.

Weakness:

- Adds a new file to the project — modest but real overhead.
- "Cultural norms" is judgment-dependent in scope.
- Existence of this manifest is itself a one-time content patch, not a process patch.

Test:

- Novelty: medium-high.
- Scrutiny survival: medium-high.
- Fertility: high (becomes referenced by N1, N4, future LOOP_DIAGNOSE candidates).
- Actionability: medium (requires drafting the manifest content).
- Mechanism independence: medium.

Verdict: REFINE → defer until N1 and N4 land. The manifest is most valuable when there is something to point at; until N1/N4 are implemented, the manifest is premature.

### Candidate N7 (Mirrors Previous M7) — Mark Prior Finding As Corrected

Shape:

Add `corrected_by: devdocs/inquiries/2026-05-04_20-38__route_memory_review_file_necessity/finding.md` to the prior `devdocs/inquiries/2026-05-04_17-56__prior_map_overlay_artifact_necessity/finding.md` frontmatter, plus a top-level Status note at the start of the body indicating the primary recommendation has been corrected and pointing at the corrected inquiry.

Strength:

- Mirrors previous LOOP_DIAGNOSE M7 pattern; same shape, same trivial cost.
- Corrected `finding.md` already has `corrects:` pointing at the prior; this completes the reciprocal link.
- Prevents a future runner reading the prior in isolation from executing the obsolete adaptive-overlay recommendation.

Weakness:

- Not a process-level patch; one-time content cleanup.

Test:

- Novelty: low (mirror of M7).
- Scrutiny survival: high.
- Fertility: low.
- Actionability: high.
- Mechanism independence: low.

Verdict: candidate (content cleanup track, parallel to M7).

### Candidate N8 (Extends Previous M8) — Continue Cross-Chain Monitoring

Shape:

Continue the previous LOOP_DIAGNOSE's M8 monitoring window (next 3 normal MVL+ runs) and add explicit observation of the cross-chain patterns P1 (Sensemaking Phase 1 axiom items) and P2 (Critique missing project-specific dimension). When the third LOOP_DIAGNOSE run completes (whichever inventory prompt is run next), assess whether P1 and P2 reach the 3+ chain threshold; if yes, promote previous M6 (cross-cutting "name-and-test" pattern) from deferred to ACTIONABLE.

Strength:

- Honors LOOP_DIAGNOSE Step 5 guardrails.
- Zero implementation cost.
- Provides the gate for M6 promotion and other cross-chain extensions.

Weakness:

- Defers cross-cutting protocol value.
- Requires the user to keep this finding visible across future LOOP_DIAGNOSE runs.

Test:

- Novelty: low (extension of M8).
- Scrutiny survival: high.
- Fertility: medium.
- Actionability: high.
- Mechanism independence: medium.

Verdict: candidate (always-on baseline, extension of M8).

### Candidate N9 — Cross-Chain Promotion Gate Documentation

Shape:

Add a one-paragraph note to `homegrown/protocols/loop_diagnose.md` Step 6 (or as a sibling section) that codifies the cross-chain promotion gate: cross-chain patterns observed at 2 of 3+ chains are tracked at Monitoring tier; promotion to ACTIONABLE requires 3+ chains; promotion to protocol-level structural change requires the LOOP_DIAGNOSE Step 6 threshold (5-10 stable findings).

Strength:

- Makes the promotion gate explicit in the protocol.
- Prevents future LOOP_DIAGNOSE runs from over-promoting cross-chain patterns.
- Single edit to one protocol file.

Weakness:

- Modifies the LOOP_DIAGNOSE protocol itself, which the protocol's own Step 5 says to do cautiously ("Do not promote LOOP_DIAGNOSE into a standalone skill or discipline until 5 to 10 diagnostic MVL+ findings show a stable internal method").
- Step 6 already names the 5-10 threshold informally; an additional 3-chain intermediate gate is new framework.

Test:

- Novelty: medium-high.
- Scrutiny survival: medium. The 3-chain intermediate gate is a useful formalization but adds protocol structure from one diagnostic chain's experience.
- Fertility: high (clarifies future LOOP_DIAGNOSE runs).
- Actionability: medium.
- Mechanism independence: low.

Verdict: REFINE → defer until at least one cross-chain pattern actually reaches 3 chains; until then, the gate is theoretical.

## Assembly Check

The strongest assembly is:

```text
N1 (Exploration cultural-norm corpus scan rule)
  + N2 (Sensemaking Phase 1 anchor-interrogation extended to Foundational Principles; strengthens M1)
  + N3 (Innovation candidate-axis decoupling rule)
  + N4 (Critique explicit-culture-fit dimension)
  + N7 (mirror M7: mark prior finding as corrected)
  + N8 (extend M8 monitoring window with cross-chain observation)
```

Emergent value:

- N1 provides the upstream corpus seed that B's principle-source verification (N2) inherits.
- N2 strengthens M1 across Constraints AND Foundational Principles, capturing both prior and current chain failure layers.
- N3 catches the candidate-set blindness independently of the upstream Sensemaking principle.
- N4 provides downstream detective coverage with a cultural-fit dimension that complements M3's duplicate-derivable-state dimension; M3 + N4 = two project-specific dimension axes for Critique.
- N7 prevents prior content execution.
- N8 provides the gate for M6 promotion when the third LOOP_DIAGNOSE run completes.

Why this assembly is stronger than any single candidate: the cascade has three upstream sources (D-prime, A, B). A patch only at one upstream layer can fail if the others are still active. The combination provides redundant coverage at the corpus, principle, candidate space, and dimension layers, plus content cleanup and monitoring extension. Each component candidate is independently evaluable by its own gate.

## Proposed V1 Shape

### Candidates To Promote (ACTIONABLE)

- N1 (Exploration cultural-norm corpus scan rule). Risk class: low. Evaluation gate: in next 3 codebase-context Exploration runs, the Coarse Scan list explicitly includes at least one cultural-norm corpus file path.
- N2 (Sensemaking anchor-interrogation extended to Foundational Principles; strengthens M1). Risk class: low. Evaluation gate: in next 3 MVL+ runs engaging Sensemaking with Foundational Principles items, at least one Phase 3 ambiguity-collapse pair targets a Phase 1 / Foundational Principles item.
- N3 (Innovation candidate-axis decoupling rule). Risk class: low. Evaluation gate: in next 3 Innovation runs evaluating problems that combine an operation with output storage, at least one candidate varies the operation-trigger axis distinctly from the storage-policy axis.
- N4 (Critique explicit-culture-fit dimension). Risk class: low. Evaluation gate: in next 3 Critique runs evaluating candidates involving project artifacts, the explicit-culture-fit (or equivalent project-norm-fit) dimension appears in the dimension list with non-trivial weight.
- N7 (mark prior finding as corrected; mirror of M7). Risk class: zero. Evaluation gate: prior `finding.md` frontmatter contains `corrected_by:` and a top-level Status note appears at the start of the body.
- N8 (extend M8 monitoring; observe cross-chain P1, P2 promotion gate). Risk class: zero. Evaluation gate: when the third LOOP_DIAGNOSE run completes, document P1/P2 chain count and promote M6 from deferred to ACTIONABLE if threshold reached.

### Candidates To Defer / Monitor (DEFERRED)

- N5 (Sensemaking Foundational-Principle source-verification at output). Gate: revive if N2 alone fails to catch the failure pattern in the next 3 MVL+ runs. Why if revived: detection-by-output is cheaper than detection-by-rule once the rule fires false negatives.
- N6 (Cultural-norms manifest file). Gate: revive after N1 and N4 land and at least one new cultural-norm reference is needed beyond `artifact_materialization.md`, `outcome_review.md`, `alignment_control.md`. Why if revived: single point of truth reduces maintenance burden on per-spec corpus lists.
- N9 (Cross-chain promotion gate documentation in LOOP_DIAGNOSE protocol). Gate: revive when at least one cross-chain pattern reaches 3+ chains AND the promotion decision needs explicit protocol guidance.
- Previous M4 (Goal-clause balance check) — D-prime evidence accumulates here; gate unchanged: revive when 3+ chains show framing imbalance.
- Previous M5 (Comparator-surfacing prompt) — no new evidence in this chain; gate unchanged.
- Previous M6 (cross-cutting "name-and-test load-bearing anchors" pattern) — at 2 of 3+ chains; promotion deferred to third LOOP_DIAGNOSE run.
- Previous M9 (canary test) — gate unchanged; no monitoring ambiguity yet.

### Always-On Baseline

- N8 / extended M8 (monitoring next 3 MVL+ runs plus cross-chain P1/P2 observation). Risk class: zero.

## Innovation Telemetry

Generators applied: 4 / 4 (Combination, Absence Recognition, Domain Transfer, Extrapolation).

Framers applied: 3 / 3 (Lens Shifting, Constraint Manipulation, Inversion).

Convergence: yes. Multiple mechanisms converge on the same core innovation: per-discipline narrow patches at corpus / principle / candidate-space / dimension layers, plus content cleanup and monitoring extension. Combination, Inversion, Absence Recognition, and Domain Transfer all point at the same compositional pattern (extend previous LOOP_DIAGNOSE candidates rather than replace; add new candidates only where the previous set did not cover).

Survivors tested: 9 / 9 candidate designs.

Failure modes observed: none. Premature evaluation avoided by generating before testing. Single-mechanism trap avoided. Early frame lock avoided. Innovation without grounding avoided. Mechanism exhaustion not reached. Survival bias not detected (multiple candidates were deferred, not all survived).

Overall: PROCEED.
