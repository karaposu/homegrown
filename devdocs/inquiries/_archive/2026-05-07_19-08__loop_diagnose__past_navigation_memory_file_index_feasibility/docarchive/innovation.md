# Innovation: Loop Diagnose - Past Navigation Memory File Index Feasibility

## User Input

LOOP_DIAGNOSE on the correction chain. Sensemaking produced 4 primary hypotheses (A Sensemaking specification gap; B Decomposition missing discovery-mechanism piece; C Innovation missing discovery-mechanism candidate; D Critique missing specification-gap prosecution). Cross-chain pattern P1 family at 5 chains × 5 layer-sub-types; P3 family at 3 chains × 3 sub-types. **TWO cross-chain promotion events triggered** (M6-refinement-S2 PROMOTION via chain-5 revival trigger; TP3 PROMOTION via P3 3-chain threshold). Q-RF reinforced (fourth specific instance: operational-discovery-gap awareness). Two-layer failure framing (chain 6 prior + chain 1 prior). Decomposition produced 13-piece question tree.

Innovation generates candidate maintenance interventions tied to primary hypotheses + documents the two cross-chain promotion events.

## Seed

Seed type: gap + cascade + two cross-chain promotion events + cumulative M6 refinement pattern + two-layer failure framing.

The gap: each of the four primary hypotheses (A, B, C, D) identifies a specific process-level absence in the prior loop's pipeline. A is the upstream specification gap for `PastNavigationMemoryFile`'s discoverability; B is the Decomposition Q-tree's missing discovery-mechanism piece; C is Innovation's missing discovery-mechanism candidate; D is Critique's missing specification-gap prosecution.

The cascade: A → B → C → D (similar shape to chains 4 and 5).

The two cross-chain promotion events:
1. **M6-refinement-S2 PROMOTION** — extends M6's Sensemaking sub-rule to cover Phase 5 / Conceptual Stabilization output (covering both proxy and specification-gap sub-types observed at this layer). Mechanically triggered per chain-5 deferral revival language. Third M6 evolution event.
2. **TP3 PROMOTION** — new cross-cutting prosecution-depth rule for Critique. Mechanically triggered per P3 family 3-chain threshold (user-perspective + failure-case + specification-gap sub-types). Second cross-cutting rule in the LOOP_DIAGNOSE corpus (after M6).

The cumulative M6 refinement pattern: chain 4 added 2 (S, C); chain 5 deferred 1 (S2); chain 6 promotes that deferred refinement. **Total M6 refinements through chain 6: 3.** Consolidation review at chain 7+ becomes more pressing but not yet triggered.

The two-layer failure framing: chain 6's diagnosis stands UPSTREAM of chain 1's separately-diagnosed failure. The corrected loop's index recommendation was a reasonable response to the prior's specification gap that selected the wrong specific candidate (per chain 1).

Valuation: highest expected benefit at the two cross-chain promotion events (they affect ALL future LOOP_DIAGNOSE runs and ALL MVL+ runs via the Sensemaking + Critique reference patches). Per-chain candidates (T1, T2, T3) target three distinct process surfaces. T7 + T8 are content cleanup + monitoring extension.

Motivation: produce 6-9 candidates; converge on 5-6 ACTIONABLE per-chain candidates + 2 cross-chain promotion events + Q-RF Reinforcement.

## Mechanism Outputs

### 1. Lens Shifting

Generic variation:

- View each maintenance candidate as a per-discipline patch.

Focused variation:

- View this chain's interventions through the cumulative-corpus lens — chain 6 is the first chain to produce TWO cross-chain promotion events in a single run. This is a corpus-level milestone. The lens-shift suggests: per-chain T-candidates (5 of them) are routine; the unusual content is the two promotion events, which should be documented separately and clearly.

Contrarian variation:

- Skip per-chain T-candidates; only document the two promotion events. Reject — per-chain hypotheses still have observable evidence and clear corrective surfaces. The promotions don't subsume the per-chain rules; they consolidate cross-chain patterns.

Surviving output:

The cumulative-corpus lens supports the structure: 5 per-chain T-candidates + 2 cross-chain promotion events (M6-refinement-S2 + TP3) + Q-RF Reinforcement + two-layer failure framing as a corpus-level observation in the verdict's reasoning.

### 2. Combination

Generic variation:

- Combine all primary-hypothesis candidates into one cross-cutting rule.

Focused variation:

- Combine T1 (Sensemaking discoverability check) with M6-refinement-S2's Phase 5 coverage. T1 is the per-chain rule for discoverability specifically; M6-refinement-S2 covers Phase 5 / Conceptual Stabilization output broadly (covering both proxy and specification-gap sub-types). They compose: T1 is the specific discoverability-check instance under M6-refinement-S2's broader Phase 5 surface.

Contrarian variation:

- Combine M6-refinement-S2 PROMOTION with potential M6 consolidation review. With three M6 refinements now (S, C, S2), one might propose consolidating to "any load-bearing concept stabilized in any Sensemaking output, anywhere in the SV1-SV6 progression, should have at least one Phase 3 ambiguity-collapse pair testing it." This would replace the per-layer enumeration with a single principle.

Reject contrarian: consolidation now is premature. The chain-7 trigger ("revisit consolidation if cumulative refinement frequency continues") respects the conservative deferral pattern.

Surviving output:

Focused combination: T1 + M6-refinement-S2 compose mechanically (T1 is specific instance; M6-refinement-S2 is broader cross-cutting rule). Document them as complementary tiers.

### 3. Inversion

Generic variation:

- Instead of patching each discipline, patch the runner to log when these patterns occur.

Focused variation:

- Instead of "Sensemaking should specify discoverability," ask "what would have to be visible for the discoverability gap to be observable at output time?" If Sensemaking SV6's stabilized model included a "Concept Discoverability Specification" subsection (for each load-bearing concept whose runtime state matters, name the determination mechanism), the gap would be observable at SV6 output time, not detected only via downstream consequences.

Contrarian variation:

- Instead of fixing Sensemaking, fix the Innovation candidate space — require Innovation to always generate at least one discovery-mechanism candidate. Reject — this would over-fire on Innovation runs where discovery isn't a load-bearing concern.

Surviving output:

Focused inversion suggests T1 should produce an explicit "Concept Discoverability Specification" output marker visible at SV6. Detection-by-output complements detection-by-process (T1's Phase 3 test).

### 4. Constraint Manipulation

Generic variation:

- Add constraint: every maintenance candidate must have an observable evaluation gate.

Focused variation:

- Add constraints for chain 6: (a) each new T-candidate is one paragraph; (b) gate fires within next 1-3 MVL+ runs; (c) M6-refinement-S2 promotion is small-scope (extension only, not replacement); (d) TP3 promotion includes the "three sub-types observed so far" disclaimer; (e) Q-RF stays Research Frontier.

Contrarian variation:

- Relax M6 consolidation deferral. With three refinements through chain 6, consolidate now. Reject — chain-7 trigger is the right pace; premature consolidation would lose specificity gained from per-layer refinements.

Surviving output:

Focused constraint set is right shape for chain 6. Both cross-chain promotion events get small-scope framing + chain-7-effectiveness-check caveats.

### 5. Absence Recognition

Generic variation:

- Missing object: a "load-bearing concept discoverability inventory" pattern in Sensemaking.

Focused variation:

- Missing object: an explicit list in Sensemaking output naming each load-bearing concept whose runtime state matters and the specified determination mechanism. T1 implements this as a Phase 3 / Phase 5 rule.

Contrarian variation:

- Missing object: a corpus-level consolidation review of cross-cutting rules at chain 7. Useful but premature this run.

Surviving output:

Focused absence reinforces T1 as the discoverability-check rule. Deferred contrarian (chain-7 consolidation) is the natural follow-up.

### 6. Domain Transfer

Generic variation:

- Transfer from API design: every operation that depends on a runtime check must specify the check's contract. The contract may be implicit if the check is platform-provided (e.g., file-existence via OS), but the dependence should still be named.

Focused variation:

- Transfer from contract programming: a Sensemaking-stabilized rule "skip if X" is a precondition; preconditions need their evaluation specified. T1 codifies this discipline.

Contrarian variation:

- Transfer from formal verification: every load-bearing concept needs a formal definition. This is too heavy for Sensemaking's discipline level; reject.

Surviving output:

Focused domain transfer reinforces T1. The discipline level matches Sensemaking's existing structure (one paragraph rule in Phase 3 + Phase 5 ambiguity-collapse).

### 7. Extrapolation

Generic variation:

- As more correction chains accumulate, M6 refinements may cumulate.

Focused variation:

- M6 has had 3 refinements through chain 6. If chain 7 surfaces another Phase X / Sensemaking layer requiring extension, the cumulative refinement count reaches 4, which would justify consolidation review. Track via T8 monitoring.

Contrarian variation:

- If TP3 needs refinement at chain 7 (e.g., a fourth P3 sub-type emerges), TP3 itself enters the same cumulative-refinement pattern. The corpus may need a "consolidation-review trigger" rule for any cross-cutting rule.

Surviving output:

Focused extrapolation produces a meta-monitoring item: track BOTH M6 refinement count AND TP3 refinement count; consolidation review at chain 7 if either reaches 4 refinements. T8 carries this.

## Candidate Designs

### Candidate T1 — Sensemaking Discoverability Check For Load-Bearing Concepts

Shape:

Add a one-paragraph rule to `homegrown/sense-making/references/sensemaking.md` Phase 3 / Ambiguity Collapse section (and Phase 5 / Conceptual Stabilization output check): when Sensemaking introduces a load-bearing concept whose USE depends on a runtime determination (e.g., *"skip if PastNavigationMemoryFile exists"* depends on existence-determination), Phase 3 must include an ambiguity-collapse pair testing how the runner determines the concept's runtime state. Phase 5 / Conceptual Stabilization output should include a brief "Concept Discoverability Specification" marker for each such load-bearing concept naming the specified determination mechanism (or explicitly noting "left to runner judgment" as a fallback).

Strength:

- Targets H1 directly at the Sensemaking surface (Phase 3 process rule + Phase 5 output marker).
- Pairs with M6-refinement-S2 (which covers Phase 5 / Conceptual Stabilization output broadly) — T1 is the specific discoverability-check instance under M6-refinement-S2's broader Phase 5 surface.
- Compositional: extends M1+N2+O1+R1 family to a fifth Sensemaking surface (specifically discoverability-check, not just concept-stabilization at the layer).
- The corrected loop's Exploration Cycle 4 (active artifact population scan) is the kind of probe T1 would have prompted earlier.

Weakness:

- "Load-bearing concept whose use depends on a runtime determination" recognition cue is judgment-dependent.
- Adds two surfaces (Phase 3 + Phase 5) to T1; cognitive load grows.
- Single-chain evidence specifically for discoverability-check (the broader Phase 5 layer pattern is at 2 chains via M6-refinement-S2's coverage of proxy + specification-gap sub-types).

Test:

- Novelty: medium-high. Builds on Premature Stabilization and Discoverability-Specification framings.
- Scrutiny survival: high.
- Fertility: high.
- Actionability: high.
- Mechanism independence: high (Lens Shifting + Combination + Inversion + Absence Recognition + Domain Transfer all converge on this candidate).

Verdict: candidate.

### Candidate T2 — Decomposition Discovery-Mechanism Inclusion Check

Shape:

Add a one-paragraph rule to `homegrown/decompose/references/decompose.md` (Step 4 / Question Tree section, OR Step 7 / Self-Evaluation Reassembly check): when Decomposition produces a question tree about an operation-policy problem with a runtime-determined trigger, the tree must include a piece that addresses the discovery/determination mechanism for any load-bearing concept the policy depends on. The Reassembly self-evaluation should explicitly check: "does the question tree have a piece for each load-bearing operational concept's runtime determination?" Failing this check is a Completeness failure mode.

Strength:

- Targets H2 directly at the Decomposition surface.
- New Decomposition surface; no previous LOOP_DIAGNOSE candidate covered Decomposition (M-N-O-R-S all targeted Sensemaking, Innovation, Critique, framing).
- The Reassembly self-evaluation check is a natural extension of existing Decomposition discipline.

Weakness:

- "Operation-policy problem with a runtime-determined trigger" recognition cue is judgment-dependent.
- New 1-chain pattern (P11); single-chain evidence.
- Decomposition cognitive load grows (Reassembly check now covers more aspects).

Test:

- Novelty: medium-high (new Decomposition surface).
- Scrutiny survival: medium-high (with single-chain caveat).
- Fertility: medium (specific to operation-policy problems).
- Actionability: medium-high.
- Mechanism independence: medium.

Verdict: candidate with explicit single-chain caveat.

### Candidate T3 — Innovation Discovery-Mechanism Candidate Generation Rule

Shape:

Add a one-paragraph rule to `homegrown/innovate/references/innovate.md` (Candidate Set / Assembly Check section): when the candidate space contains operation-policy candidates with runtime-determined triggers, generate at least one candidate that addresses the discovery mechanism for the trigger's load-bearing concept. The discovery-mechanism candidate options include: explicit search contract, maintained registry, input metadata declaration, generated cache, and "left to runner judgment" with caveats. The Assembly Check should explicitly test whether the discovery-mechanism axis has been covered.

Strength:

- Targets H3 directly at the Innovation surface.
- Composes with previous N3 (operation-storage axis decoupling), R3 (phase-conditional candidates), S3 (differential-classifier candidate generation) — different axis (discovery mechanism).
- The corrected loop's Innovation generated multiple discovery-mechanism candidates (Global Index, Append-Only Event Log, Per-File Metadata, Hybrid Registry, Consumed-Set) — demonstrating the rule's practical applicability.

Weakness:

- "Operation-policy candidate with runtime-determined trigger" recognition cue is judgment-dependent.
- May overlap with N3 / R3 / S3 in some inquiries; differentiation requires care.
- Largely inherited from upstream (T1's Sensemaking specification); independent component is candidate-set blind spot.

Test:

- Novelty: medium-high.
- Scrutiny survival: medium-high.
- Fertility: medium-high.
- Actionability: medium-high.
- Mechanism independence: medium-high.

Verdict: candidate.

### Candidate T7 — Mark Prior Finding As Corrected (Mirrors M7+N7+O7+R7+S7)

Shape:

Add `corrected_by: devdocs/inquiries/2026-05-06_07-06__past_navigation_memory_file_index_feasibility/finding.md` to the prior `devdocs/inquiries/2026-05-05_20-02__early_stage_always_full_route_memory_review/finding.md` frontmatter. Add a top-level Status note at the start of the body indicating the primary recommendation has been corrected (with discovery-mechanism specification in the corrected loop, then further corrected by chain-1's diagnosis to favor search over maintained index — the two-layer failure framing should be acknowledged in the Status note).

Strength:

- Mirrors M7+N7+O7+R7+S7 pattern; consistent across diagnostic chains.
- Trivial cost.
- The Status note explicitly references the two-layer correction structure (chain 6 prior → chain 1 prior → chain 1 corrected), which is a corpus-level observation worth preserving.

Weakness:

- Not process-level prevention.
- The two-layer Status note is more nuanced than previous M7-S7 patterns (which annotated single corrections); requires judgment to phrase correctly.

Test:

- Novelty: low (mirror) but with two-layer twist.
- Scrutiny survival: high.
- Fertility: low.
- Actionability: high.
- Mechanism independence: low.

Verdict: candidate.

### Candidate T8 — Continue Monitoring + M6 Cumulative Refinement Frequency + TP3 Effectiveness + P11 Tracking + Signal-Type + Corpus-Level (Extends M8+N8+O8+R8+S8)

Shape:

Continue M8+N8+O8+R8+S8 monitoring (next 3 normal MVL+ runs) and add (a) M6 cumulative refinement count (3 through chain 6; consolidation review trigger at 4); (b) TP3 effectiveness tracking (does the new cross-cutting prosecution-depth rule fire correctly across user-perspective + failure-case + specification-gap sub-types?); (c) P11 (Decomposition missing discovery-mechanism piece) at 1 chain — gate at 3+ chains for cross-cutting rule; (d) signal-type observation: this is the sixth distinct expression style (question-style); (e) corpus-level pattern: chain 6 is the third instance of "chain N's prior = chain M's corrected" pattern — track for corpus-graph stability; (f) two-layer failure framing — track whether future chains exhibit similar upstream-downstream diagnostic dependencies.

Strength:

- Extends M8+N8+O8+R8+S8 with chain-6's specific tracking dimensions.
- Zero implementation cost.
- Provides chain-7 consolidation review trigger AND TP3 first-application effectiveness check.

Weakness:

- Multiple monitoring tracks (M6 + TP3 + P11 + signal-type + corpus-level + two-layer framing) require ongoing observation across LOOP_DIAGNOSE runs.

Test:

- Novelty: low (extension).
- Scrutiny survival: high.
- Fertility: medium.
- Actionability: high.
- Mechanism independence: medium.

Verdict: candidate (always-on baseline).

### Candidate M6-refinement-S2 PROMOTION — Extend M6 Sensemaking Sub-Rule To Phase 5 / Conceptual Stabilization Output

Shape:

Refine M6's Sensemaking sub-rule from (current state with chain-4 refinement-S):

> "for each load-bearing concept stabilized in Sensemaking output (whether at Phase 1 / Constraints, Phase 1 / Foundational Principles, SV2+ terminology, OR Phase 2 / Perspective Checking when the candidate involves project-state-dependent rules), generate at least one ambiguity-collapse pair testing the strongest counter-interpretation"

to:

> "...OR Phase 5 / Conceptual Stabilization output (trigger-classifier rules and final-output concepts, including both proxy-vs-structural distinctions and load-bearing-concept discoverability specifications)..."

Strength:

- **Mechanically grounded in chain-5 deferral revival trigger:** *"revive at chain 6 if P1 family extends to a 5th layer or if the trigger-classifier-rule layer pattern recurs."* Chain-6's specification-gap instance is at the Phase 5 layer — same layer as chain-5's proxy instance. Layer-level recurrence trigger met.
- Small-scope refinement (one extension to existing Sensemaking sub-rule).
- P1 family at 5 chains × 5 layer-sub-types provides strong cross-chain support.
- Covers both observed Phase 5 sub-types (proxy + specification-gap).
- **Third M6 evolution event** (after chain-3 promotion + chain-4 refinements S, C).

Weakness:

- Cumulative M6 refinement count: 3. Approaching consolidation territory but not yet triggered.
- Refinement piecemeal vs consolidation tradeoff: each refinement stays small, but cumulative refinements may eventually require rule-shape consolidation. Current heuristic: consolidation review at chain 7+ if refinement count reaches 4.
- The refinement adds another specific-layer enumeration to the Sensemaking sub-rule; future refinements at additional layers would compound this.

Test:

- Novelty: medium (third refinement event; pattern of M6 evolution is now established).
- Scrutiny survival: high (chain-5 deferral trigger explicitly named the revival condition).
- Fertility: high (covers Phase 5 layer for future LOOP_DIAGNOSE runs).
- Actionability: medium-high.
- Mechanism independence: medium (specific mechanically-triggered event from chain-5 deferral language).

Verdict: PROMOTE from DEFERRED to ACTIONABLE with explicit small-scope + chain-7-effectiveness-check + cumulative-refinement-count caveats.

### Candidate TP3 PROMOTION — Cross-Cutting Prosecution-Depth Rule For Critique

Shape:

Add a one-paragraph rule to `homegrown/td-critique/references/td-critique.md` (Phase 2 / Adversarial Evaluation section): when constructing prosecution against a candidate, in addition to dimension-level objections, explicitly construct prosecution at the appropriate depth-axes when applicable:

```text
(a) user-perspective objection — read `_branch.md` Source Input and construct at least one objection that addresses a user-stated concern at the level expressed (per chain-3 O3).

(b) specific failure-case scenario — for trigger-policy candidates, construct at least one edge case where the candidate's classifier might allow the failure mode the candidate is supposed to prevent (per chain-5 S2).

(c) operational-specification-gap probe — for candidates with runtime-determined triggers, probe whether the rule's load-bearing concepts have specified determination mechanisms (per chain-6 D / TP3).

The runner should pick the depth-axes most relevant to the candidate's risk surface. Not every axis applies to every Critique run; the rule requires consideration of relevance, not application of all axes.
```

Strength:

- **Mechanically grounded in P3 family 3-chain threshold.** Three chains × three sub-types (user-perspective + failure-case + specification-gap) of the same meta-pattern (prosecution applied at insufficient depth within existing dimensions).
- **Second cross-cutting rule in the LOOP_DIAGNOSE corpus** (after M6).
- Subsumes the per-chain prosecution-depth rules' shared meta-property without replacing them. Per-chain candidates O3 + S2 + (chain-6 D's prosecution-gap observation) remain valid as concrete instances; TP3 is the formalization.
- Composes with existing td-critique.md structure (extends Phase 2 / Adversarial Evaluation with a depth-axis check).

Weakness:

- 3 chains is the trigger but a small sample. Risk of over-fitting the rule to the three observed sub-types.
- "Pick the depth-axes most relevant" is judgment-dependent — the rule requires the runner to assess which axes apply, which adds cognitive load.
- Future chains may surface additional sub-types (a fourth, fifth, etc.). The rule may need refinement events analogous to M6's.

Test:

- Novelty: medium-high (new cross-cutting rule).
- Scrutiny survival: high (P3 3-chain threshold + three observable sub-types).
- Fertility: high (covers Critique prosecution depth for all future LOOP_DIAGNOSE runs).
- Actionability: medium-high.
- Mechanism independence: high (Lens Shifting + Combination + Absence Recognition + Domain Transfer all converge).

Verdict: PROMOTE as ACTIONABLE with explicit "three sub-types observed so far; future sub-types may emerge" caveat AND "rule requires consideration of relevance, not application of all axes" disclaimer.

### Candidate Q-RF Reinforcement (Research Frontier, NOT a candidate)

Shape (NOT a maintenance candidate):

Reinforce Q-RF with fourth specific instance. Four instances across four chains:
- Chain 3 metacognitive ("feels messy") — system can't detect when its own output feels not-clean.
- Chain 4 calibration-awareness — system can't detect when its rule depends on calibration the system does not have.
- Chain 5 proxy-vs-structural-awareness — system can't distinguish proxy from structural distinction.
- Chain 6 operational-discovery-gap awareness — system can't detect when its rule depends on operational determination it doesn't specify.

Same underlying capability gap; four specific instances. Per `enes/desc.md` Predictive RC architecture; multi-phase architectural work; out of scope for per-paragraph patches.

Verdict: SURVIVE as Research Frontier reinforcement (fourth specific instance). NOT a maintenance candidate.

### Candidate Two-Layer Failure Framing (Documentation Note, NOT a candidate)

Shape (NOT a maintenance candidate; documented in finding's Reasoning section):

Chain 6's diagnosis stands UPSTREAM of chain 1's separately-diagnosed failure. The corrected loop's index recommendation was a reasonable response to the prior's specification gap (chain 6's diagnosis identified that gap). The corrected loop's specific candidate (maintained index) was structurally over-engineered relative to actual need (chain 1's diagnosis). Both diagnoses stand independently.

Corpus-level observation: chains can have layered failure dependencies. Chain N's diagnostic can stand UPSTREAM of chain M's separately-diagnosed failure, where chain M corrects the immediate downstream consequence and chain N corrects the upstream cause. Future LOOP_DIAGNOSE runs should be aware of this possibility.

Verdict: NOT a maintenance candidate; documented in finding's Reasoning section as a corpus-level observation. Tracked in T8 monitoring.

## Assembly Check

The strongest assembly is:

```text
T1 (Sensemaking discoverability check; Phase 3 + Phase 5 surfaces)
  + T2 (Decomposition discovery-mechanism inclusion check; new P11)
  + T3 (Innovation discovery-mechanism candidate generation rule)
  + T7 (Mark prior finding corrected with two-layer Status note; mirrors M7+N7+O7+R7+S7)
  + T8 (Extend M8+N8+O8+R8+S8 monitoring with M6 cumulative refinement + TP3 effectiveness + P11 + signal-type + corpus-level + two-layer framing tracks)
  + M6-refinement-S2 PROMOTION (extend M6 Sensemaking sub-rule to Phase 5 / Conceptual Stabilization output; THIRD M6 evolution event)
  + TP3 PROMOTION (cross-cutting prosecution-depth rule for Critique; SECOND cross-cutting rule in LOOP_DIAGNOSE corpus)
  + Q-RF Reinforcement (Research Frontier, fourth specific instance)
  + Two-Layer Failure Framing (corpus-level observation in Reasoning, NOT a candidate)
```

Emergent value:

- T1 + M6-refinement-S2 cover Sensemaking specification-gap detection at the per-discipline patch + cross-cutting refinement layers. T1 is specific (discoverability check); M6-refinement-S2 is broader (Phase 5 layer, both proxy and specification-gap sub-types).
- T2 opens a NEW Decomposition surface no previous LOOP_DIAGNOSE candidate covered. The corpus's per-discipline coverage now spans Sensemaking (M1+N2+O1+R1+T1), Decomposition (T2), Innovation (N3+R3+S3+T3), and Critique (M3+N4+O2+R2+TP3 cross-cutting).
- T3 catches Innovation's discovery-mechanism candidate gap at per-chain layer; cross-chain promotion deferred (P11 at 1 chain only).
- T7 + T8 maintain consistency with previous chains plus add chain-6-specific tracking dimensions.
- M6-refinement-S2 PROMOTION is the third M6 evolution event; cumulative refinement count of 3 approaches consolidation territory.
- TP3 PROMOTION is the second cross-cutting rule in the LOOP_DIAGNOSE corpus. The corpus now has two cross-cutting rules (M6 + TP3) plus the documented N9 protocol-level promotion-gate codification (chain 5).
- Q-RF reinforcement surfaces fourth specific instance of the system-level capability gap.
- Two-layer failure framing is documented as corpus-level observation in the finding's Reasoning, not a maintenance candidate.

Assembly verdict: SURVIVE.

Refinements required before implementation:

- T1 must explicitly cite "load-bearing concept whose use depends on runtime determination" recognition cue + Phase 3 + Phase 5 dual-surface application.
- T2 must reference Decomposition Step 7 / Self-Evaluation Reassembly check + single-chain caveat.
- T3 must distinguish from N3 / R3 / S3 (different axes) + reference Assembly Check phase.
- T7 reciprocal annotation + two-layer Status note acknowledging chain 1's separate diagnosis.
- T8 references separate tracks for M6 cumulative refinement + TP3 effectiveness + P11 + signal-type + corpus-level + two-layer framing.
- M6-refinement-S2 PROMOTION cites chain-5 deferral revival language verbatim + small-scope caveat + chain-7-effectiveness-check.
- TP3 PROMOTION cites P3 family 3-chain threshold + three observed sub-types + "future sub-types may emerge" caveat.
- Q-RF Reinforcement Research Frontier framing.
- Two-layer failure framing in Reasoning section, NOT in Candidates section.

## Proposed V1 Shape

### ACTIONABLE Per-Chain Candidates

- **T1** (Sensemaking discoverability check; Phase 3 + Phase 5 dual-surface). Risk class: low. Gate: in next 3 MVL+ runs producing Sensemaking outputs with load-bearing concepts whose use depends on runtime determination, T1 fires (Phase 3 ambiguity-collapse pair + Phase 5 output marker).

- **T2** (Decomposition discovery-mechanism inclusion check; new P11 family). Risk class: low. Gate: in next 3 MVL+ runs producing Decomposition Q-trees about operation-policy problems with runtime-determined triggers, T2's Reassembly check fires. **Single-chain caveat (P11 at 1 chain); revisit if not effective.**

- **T3** (Innovation discovery-mechanism candidate generation rule). Risk class: low. Gate: in next 3 Innovation runs evaluating operation-policy candidates with runtime-determined triggers, at least one discovery-mechanism candidate variant is generated.

- **T7** (mark prior finding corrected with two-layer Status note; mirrors M7+N7+O7+R7+S7). Risk class: zero. Gate: prior `finding.md` annotated with `corrected_by:` + Status note explicitly noting chain 1's separate diagnosis.

- **T8** (extend M8+N8+O8+R8+S8 monitoring with chain-6 tracking dimensions). Risk class: zero. Gate: at chain 7 completion, document M6 cumulative refinement count, TP3 effectiveness, P11 chain count, signal-type observation, corpus-level pattern stability, two-layer framing recurrence.

### ACTIONABLE Cross-Chain Promotion Events

- **M6-refinement-S2 PROMOTION** (extend M6 Sensemaking sub-rule to Phase 5 / Conceptual Stabilization output). Risk class: low. Gate: in next 3 MVL+ runs where Sensemaking encounters trigger-classifier rules or load-bearing-concept stabilization at Phase 5, M6's refined rule fires at the Phase 5 surface. **Small-scope caveat + chain-7-effectiveness-check + cumulative-refinement-count caveat.** Third M6 evolution event.

- **TP3 PROMOTION** (cross-cutting prosecution-depth rule for Critique; second cross-cutting rule in LOOP_DIAGNOSE corpus). Risk class: low. Gate: in next 3 Critique runs against candidates where prosecution-depth-axes apply (user-perspective / failure-case / specification-gap), TP3's relevance-check fires. **"Three sub-types observed so far; future sub-types may emerge" caveat.**

### Always-On Baseline

- **T8** (extended monitoring with chain-6 tracking dimensions). Risk class: zero.

### Research Frontier (Not A Candidate)

- **Q-RF Reinforcement** (quality-awareness gap, fourth specific instance: operational-discovery-gap awareness). Per `enes/desc.md` Predictive RC architecture; out of scope for per-paragraph patches.

### Documentation Note (Not A Candidate)

- **Two-Layer Failure Framing** (corpus-level observation in Reasoning section). Chain 6's diagnosis stands UPSTREAM of chain 1's separately-diagnosed failure. Both diagnoses stand independently.

### DEFERRED

- M6 consolidation review (consolidate Sensemaking sub-rule from per-layer enumeration to "any load-bearing concept stabilized in any Sensemaking output, anywhere in the SV1-SV6 progression"). Gate: revisit at chain 7 if cumulative M6 refinement count reaches 4. Why if revived: cumulative refinement burden suggests rule-shape consolidation may be more sustainable than continuing piecemeal layer-by-layer extensions.

- Cross-cutting Decomposition completeness rule (combining T2 with future Decomposition completeness candidates). Gate: revive at 3+ chains for P11 family or related Decomposition patterns.

- Previous M4, M5, M9, N5, N6, O3-overlap, O4, O5/O6, R4 — gates unchanged.

- New patterns P5, P6, P7, P8, P11 at 1 chain — gate: 3+ chains for cross-cutting candidate.

## Innovation Telemetry

Generators applied: 4 / 4 (Combination, Absence Recognition, Domain Transfer, Extrapolation).

Framers applied: 3 / 3 (Lens Shifting, Constraint Manipulation, Inversion).

Convergence: yes. Multiple mechanisms converge on per-discipline narrow patches at discoverability-check + discovery-mechanism-piece + discovery-mechanism-candidate + prosecution-depth layers, plus content cleanup, monitoring extension, two cross-chain promotion events, Q-RF reinforcement, and two-layer failure framing as corpus-level observation. Combination, Inversion, Absence Recognition, and Domain Transfer all support the compositional pattern (extend previous + add new where uncovered + promote cross-chain when threshold met + document corpus-level observations).

Survivors tested: 9 candidate designs (T1, T2, T3, T7, T8, M6-refinement-S2 PROMOTION, TP3 PROMOTION, Q-RF Reinforcement as Research Frontier, Two-Layer Failure Framing as corpus-level observation).

Failure modes observed: none. Premature evaluation avoided. Single-mechanism trap avoided. Early frame lock avoided. Innovation without grounding avoided. Mechanism exhaustion not reached. Survival bias checked (T2 single-chain caveat; M6-refinement-S2 + TP3 small-scope caveats; Q-RF stays Research Frontier; two-layer framing stays observation).

Overall: PROCEED.
