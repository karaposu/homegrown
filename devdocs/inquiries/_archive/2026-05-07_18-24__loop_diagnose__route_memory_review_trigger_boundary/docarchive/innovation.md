# Innovation: Loop Diagnose - Route Memory Review Trigger Boundary

## User Input

LOOP_DIAGNOSE on the correction chain. Sensemaking produced 4 primary hypotheses (D framing, A Sensemaking proxy-as-structural at trigger-classifier-rule layer, C Innovation uniform-bounded, B Critique prosecution at abstract level); CONCLUDE ruled out. P1 family extends to 4 chains × 4 layers; P3 family extends to 2 chains × 2 sub-types. M6 effectiveness check #2 documented; M6-refinement-S2 deferred. **N9 promotion mechanically triggered (5-finding threshold met).** Q-RF reinforced with third specific instance. Decomposition produced 11-piece question tree.

Innovation generates candidate maintenance interventions tied to primary hypotheses + N9 promotion documentation.

## Seed

Seed type: gap + cascade + N9 promotion event + cumulative M6 refinement pattern.

The gap: each of the three primary hypotheses (A, B, C — D is observation-only) identifies a specific process-level absence. N9 promotion is the protocol-level event triggered by the 5-finding threshold.

The cascade: D → A → C → B.

The N9 promotion event is the FIRST protocol-level change in the LOOP_DIAGNOSE corpus. Innovation must produce a small-scope codification.

The cumulative M6 refinement pattern: chain 4 added 2 refinements; chain 5 surfaces a third surface (Phase 5 / Conceptual Stabilization) but defers. Future chains should observe whether this pattern is stable or signals rule-shape consolidation need.

Valuation: highest expected benefit per byte at N9 promotion (one protocol-section addition; clarifies cross-chain pattern gates for all future runs). Per-chain candidates (S1, S2, S3) target three distinct process surfaces.

Motivation: produce 6-8 candidates; converge on 5 ACTIONABLE per-chain + N9 PROMOTION + Q-RF Reinforcement.

## Mechanism Outputs

### 1. Lens Shifting

Generic: each candidate as per-discipline patch.

Focused: view this chain's interventions through the cumulative-corpus lens — N9 promotion is the corpus's first protocol-level change event; per-chain candidates accumulate as before.

Contrarian: skip per-chain patches; only N9 promotion. Rejected — per-chain hypotheses still have observable evidence and clear corrective surfaces.

Surviving: cumulative-corpus lens supports BOTH per-chain candidates AND N9 promotion as complementary tiers.

### 2. Combination

Generic: combine all primary-hypothesis candidates into one cross-cutting rule.

Focused: combine S1 (proxy-vs-structural test for trigger-classifier rules) with the deferred M6-refinement-S2 (extend to Phase 5 / Conceptual Stabilization). S1 is the per-chain instance; M6-refinement-S2 would be the formalization. Defer M6-refinement-S2; promote S1 as ACTIONABLE.

Contrarian: combine N9 promotion with another protocol-level change (e.g., codify the entire compositional candidate scheme). Rejected — single protocol-level event per chain is the right pace.

Surviving: focused combination supports S1 promotion now + M6-refinement-S2 deferral.

### 3. Inversion

Generic: instead of patching each discipline, patch the runner to log when these patterns occur.

Focused: instead of "Critique should construct failure cases," ask "what would have to be visible for failure-case-absence to be observable?" If Critique's prosecution structure included an explicit "Failure Cases Constructed" subsection, the absence would be observable at output time.

Contrarian: instead of fixing per-discipline, fix the user input. Reject (single-chain evidence; runner-level changes still need cross-chain accumulation).

Surviving: focused inversion suggests S2 should require an explicit "Failure Cases Constructed" output marker.

### 4. Constraint Manipulation

Generic: every candidate has observable evaluation gate.

Focused: this chain's constraints — (a) one-paragraph patches; (b) gates within next 3 MVL+ runs; (c) M6-refinement-S2 deferred with chain-6 revival trigger; (d) N9 promotion small-scope; (e) Q-RF stays Research Frontier.

Contrarian: relax M6-refinement-S2 deferral. Reject — cumulative refinement risk.

Surviving: focused constraint set is right shape for chain 5.

### 5. Absence Recognition

Generic: missing object — a "structural-vs-proxy distinguisher" pattern in Sensemaking.

Focused: missing object — an Innovation rule that distinguishes "candidate's classifier uses incidental property as proxy" from "candidate's classifier uses actual structural dependency." S3 targets this; phrasing must be concrete.

Contrarian: missing — a corpus-level retrospective tool. Defer (premature at chain 5).

Surviving: focused absence reinforces S3.

### 6. Domain Transfer

Generic: import from type theory: phantom types vs nominal types; structural typing vs nominal typing.

Focused: trigger-classifier rules in Sensemaking are like type tags — they should be checked for whether they are nominal (incidental labels) vs structural (actual property-dependence). The structural-vs-proxy test (S1) is essentially a nominal-vs-structural type check applied to trigger logic.

Contrarian: import from regulatory law: rules with incidental triggers fail under edge cases. Reject as too broad.

Surviving: focused domain transfer reinforces S1's structural-vs-proxy test framing.

### 7. Extrapolation

Generic: as more correction chains accumulate, M6 may need consolidation.

Focused: M6 has been refined twice (chain 4); a potential third refinement (chain 5) is deferred. If chain 6 surfaces a fifth Sensemaking-output layer requiring M6 extension, the cumulative refinement burden suggests M6 should consolidate to a broader principle ("any load-bearing concept stabilized in any Sensemaking output, anywhere in the SV1-SV6 progression"). Track M6 refinement frequency; consolidate at chain 7+ if pattern continues.

Contrarian: M6 may be fundamentally wrong-shaped. Reject prematurely; allow refinement evolution.

Surviving: focused extrapolation produces a meta-monitoring item: track M6 cumulative refinement count; revisit consolidation at chain 7-10.

## Candidate Designs

### Candidate S1 — Sensemaking Proxy-Vs-Structural Test For Trigger-Classifier Rules

Shape:

Add a one-paragraph rule to `homegrown/sense-making/references/sensemaking.md` Phase 5 / Conceptual Stabilization section: when Sensemaking produces a trigger-classifier rule (a rule that determines when an operation runs vs when it doesn't, especially using categorical labels like "bounded vs project-level" or "small vs large"), test whether the classifier uses incidental input properties as proxies for the actual structural dependency. Ask: "if I replace the classifier's category labels with their underlying structural property, does the rule still make sense? If not, the original classifier was a proxy." Failing to apply this test when trigger-classifier rules are produced is a Conceptual Stabilization failure mode.

Strength: targets H1 at the new Sensemaking surface (Phase 5 / trigger-classifier rules); P1 fourth instance supports the rule; structurally distinct from M1+N2+O1+R1.

Weakness: "trigger-classifier rule" recognition cue is judgment-dependent; the test asks for a counter-factual which can be hard to construct.

Test: Novelty: medium-high. Scrutiny survival: high (P1 fourth instance). Fertility: high. Actionability: high. Mechanism independence: high.

Verdict: candidate.

### Candidate S2 — Critique Prosecution Failure-Case Construction Rule (Sister To O3)

Shape:

Add a one-paragraph rule to `homegrown/td-critique/references/td-critique.md` (Phase 2 / Adversarial Evaluation section): when constructing prosecution against a candidate, in addition to addressing user-stated concerns from `_branch.md` Source Input (per O3), explicitly construct at least one specific failure-case scenario where the candidate's logic might fail. For trigger-policy candidates, this means constructing edge cases where the trigger's classifier might allow the failure mode the candidate is supposed to prevent. **Sister rule to O3 (chain 3 user-perspective check); P3 family second instance.**

Strength: targets H2 (P3 second instance); pairs with O3 as two sub-rules covering prosecution-depth at two sub-types.

Weakness: single-chain evidence (P3 at 2 chains overall but each sub-type at 1 chain); recognition cue ("specific failure-case scenario") is judgment-dependent; may overlap with O3 in some inquiries.

Test: Novelty: medium. Scrutiny survival: medium-high (with single-chain caveat). Fertility: medium-high. Actionability: medium-high. Mechanism independence: medium.

Verdict: candidate with explicit single-chain caveat AND "pairs with O3" note.

### Candidate S3 — Innovation Differential-Classifier Candidate Generation Rule

Shape:

Add a one-paragraph rule to `homegrown/innovate/references/innovate.md` (Candidate Set / Assembly Check section): when Innovation generates trigger-policy candidates (rules that determine when an operation runs), generate at least one variant that uses a structural-dependency classifier (e.g., "does this run depend on prior route memory?") instead of an input-property classifier (e.g., "is this run bounded vs project-level?"). The two candidate types should be tested against each other in the Test phase.

Strength: targets H3 (Innovation uniform-bounded); new Innovation surface; orthogonal to N3 (operation-storage axis decoupling) and R3 (phase-conditional candidates).

Weakness: "trigger-policy candidate" recognition cue is judgment-dependent; the rule may overlap with R3 (phase-conditional candidate generation).

Test: Novelty: medium-high. Scrutiny survival: medium-high. Fertility: medium-high. Actionability: medium-high. Mechanism independence: medium-high.

Verdict: candidate.

### Candidate S7 — Mark Prior Finding As Corrected (Mirrors M7+N7+O7+R7)

Shape:

Add `corrected_by: devdocs/inquiries/2026-05-05_17-12__route_memory_review_trigger_boundary/finding.md` to the prior `devdocs/inquiries/2026-05-04_20-38__route_memory_review_file_necessity/finding.md` frontmatter. Add a top-level Status note.

Strength: mirrors M7+N7+O7+R7 pattern; consistent across chains; trivial cost.

Weakness: not process-level prevention.

Test: Novelty: low. Scrutiny survival: high. Fertility: low. Actionability: high. Mechanism independence: low.

Verdict: candidate.

### Candidate S8 — Continue Monitoring + M6 Refinement Frequency + P1 4-Layer + P3 2-Sub-Type + Signal-Type Tracks (Extends M8+N8+O8+R8)

Shape:

Continue M8+N8+O8+R8 monitoring (next 3 normal MVL+ runs) and add (a) M6 cumulative refinement frequency (chain 4 added 2; chain 5 deferred 1; track at chain 6+); (b) P1 family 4-layer tracking (note: M6's current shape with chain-4 refinements covers 4-of-5 layers; chain-5 trigger-classifier layer not covered); (c) P3 family 2-sub-type tracking (cross-cutting promotion at 3+ chains); (d) signal-type observation: this is the fifth distinct expression style (hybrid metacognitive + structural); (e) corpus-level pattern observation: chain 5 prior = chain 2 corrected, chain 5 corrected = chain 3 prior — second instance of "same artifact serves both roles across chains" pattern; (f) M6 consolidation review: at chain 7+, assess whether M6's rule shape needs consolidation given cumulative refinement count.

Strength: extends M8+N8+O8+R8 with new cross-chain pattern + M6 evolution + corpus-level observation tracks. Zero implementation cost.

Weakness: many monitoring tracks require ongoing observation.

Test: Novelty: low (extension). Scrutiny survival: high. Fertility: medium. Actionability: high. Mechanism independence: medium.

Verdict: candidate (always-on baseline).

### Candidate N9 PROMOTION — LOOP_DIAGNOSE Protocol Promotion-Gate Documentation

Shape:

Add a one-paragraph note to `homegrown/protocols/loop_diagnose.md` Step 6 (or as a sibling section) documenting the cross-chain pattern promotion gate explicitly:

```text
Cross-chain pattern promotion gates:
- 1 chain instance: pattern enters Monitoring tier with revival trigger (3+ chains for cross-cutting candidate revival).
- 3+ chain instances: cross-cutting candidate becomes ACTIONABLE per its specific revival trigger language.
- 5-10 chain corpus: protocol-level changes to LOOP_DIAGNOSE itself become plausible (this gate).
- Cumulative refinement frequency: if a recently-promoted ACTIONABLE rule (e.g., M6) requires refinement at multiple successive chains, consolidate the rule shape rather than continuing to refine.

LOOP_DIAGNOSE protocol changes (e.g., this Step 6 note, future protocol-level codifications) are themselves subject to the 5-10 finding gate. As of this finding (chain 5), the lower bound is met; protocol changes are documented with explicit "revisit at chain 7-10" gates.
```

Strength: 5-finding threshold met (this is finding 5); previous deferral language ("revive when at least one cross-chain pattern reaches 3+ chains AND the promotion decision needs explicit protocol guidance") is met (P1+P2 each at 4; M6 promotion + 2 refinements); first protocol-level change event in the LOOP_DIAGNOSE corpus.

Weakness: 5 findings is the LOWER bound; promoting at minimum is borderline. Mitigated by small-scope codification + explicit chain-7-10 revisit gate.

Test: Novelty: medium. Scrutiny survival: high (mechanically triggered). Fertility: high (clarifies future LOOP_DIAGNOSE runs). Actionability: high. Mechanism independence: low (specific mechanically-triggered event).

Verdict: PROMOTE from DEFERRED to ACTIONABLE with explicit small-scope + chain-7-10-revisit caveats.

### Candidate M6-refinement-S2 — DEFERRED (Extend M6 Sensemaking Sub-Rule To Phase 5 / Conceptual Stabilization)

Shape (deferred):

Refine M6's Sensemaking sub-rule from (current state with refinement-S):

> "for each load-bearing concept stabilized in Sensemaking output (whether at Phase 1 / Constraints, Phase 1 / Foundational Principles, SV2+ terminology, OR Phase 2 / Perspective Checking when the candidate involves project-state-dependent rules), generate at least one ambiguity-collapse pair testing the strongest counter-interpretation"

to (proposed):

> "...OR Phase 5 / Conceptual Stabilization output (trigger-classifier rules and final-output concepts)..."

Verdict: DEFERRED with revival trigger: revive at chain 6 if P1 family extends to a 5th layer or if the trigger-classifier-rule layer pattern recurs. Cumulative M6 refinement burden (chain 4 added 2; chain 5 would add 1) signals possible consolidation at chain 7+. Conservative deferral.

### Candidate Q-RF Reinforcement (Research Frontier, NOT a candidate)

Shape (NOT a maintenance candidate):

Reinforce Q-RF with third specific instance. Three instances across three chains: chain 3 metacognitive ("feels messy"); chain 4 calibration-awareness ("can't judge material effect when uncalibrated"); chain 5 proxy-vs-structural-awareness ("can't distinguish proxy from structural distinction"). Per `enes/desc.md` Predictive RC architecture; multi-phase architectural work; out of scope for per-paragraph patches.

Verdict: SURVIVE as Research Frontier reinforcement (third specific instance). NOT a maintenance candidate.

## Assembly Check

Strongest assembly:

```text
S1 (Sensemaking proxy-vs-structural test for trigger-classifier rules)
  + S2 (Critique prosecution failure-case construction; sister to O3; single-chain caveat)
  + S3 (Innovation differential-classifier candidate generation rule)
  + S7 (mark prior finding corrected; mirrors M7+N7+O7+R7)
  + S8 (extend M8+N8+O8+R8 monitoring with new tracks)
  + N9 PROMOTION (LOOP_DIAGNOSE protocol promotion-gate documentation; 5-finding threshold met)
  + M6-refinement-S2 DEFERRED (revisit at chain 6)
  + Q-RF Reinforcement (Research Frontier, third specific instance)
```

Emergent value:

- S1 + (deferred M6-refinement-S2) cover the trigger-classifier-rule layer at per-chain patch + cross-cutting deferral layers.
- S2 + previous O3 cover prosecution-depth at two sub-types (failure-case + user-perspective).
- S3 catches Innovation candidate-set blind spot at per-chain layer.
- S7 + S8 maintain consistency.
- **N9 promotion** is the first protocol-level change event in the LOOP_DIAGNOSE corpus.
- Q-RF reinforcement surfaces third specific instance.

Assembly verdict: SURVIVE.

Refinements required:

- S1 must explicitly cite "Phase 5 / Conceptual Stabilization output (trigger-classifier rules)."
- S2 must reference O3 as sister rule and include single-chain caveat.
- S3 must distinguish from N3 (operation-storage axis decoupling) and R3 (phase-conditional candidates).
- S7 reciprocal annotation only.
- S8 references separate cross-chain + M6 evolution + corpus-level observation tracks.
- N9 promotion small-scope + chain-7-10-revisit gate.
- M6-refinement-S2 deferred with chain-6 revival trigger.
- Q-RF Reinforcement Research Frontier framing.

## Proposed V1 Shape

### ACTIONABLE Per-Chain Candidates

- S1 (Sensemaking proxy-vs-structural test for trigger-classifier rules). Risk class: low. Gate: in next 3 MVL+ runs producing trigger-classifier rules, the test is applied.
- S2 (Critique prosecution failure-case construction; sister to O3). Risk class: low-medium. Gate: in next 3 Critique runs against trigger-policy candidates, at least one failure-case scenario is constructed. **Single-chain caveat (P3 second sub-type at 1 chain); revisit if not effective in next 3 runs.**
- S3 (Innovation differential-classifier candidate generation). Risk class: low. Gate: in next 3 Innovation runs producing trigger-policy candidates, at least one structural-dependency-classifier variant is generated.
- S7 (mark prior finding corrected). Risk class: zero. Gate: prior `finding.md` annotated.
- S8 (extend monitoring with M6 refinement frequency + P1 4-layer + P3 2-sub-type + signal-type + corpus-level tracks). Risk class: zero. Gate: at chain 6 completion, document tracks.

### ACTIONABLE Protocol-Level Promotion

- **N9 PROMOTION** (LOOP_DIAGNOSE protocol promotion-gate documentation). Risk class: low. Gate: protocol file edit verified by single read; revisit codification at chain 7-10.

### Deferred

- M6-refinement-S2 (extend M6 Sensemaking sub-rule to Phase 5). Gate: revive at chain 6 if P1 family extends to a 5th layer or if the trigger-classifier-rule layer pattern recurs.
- Cross-cutting prosecution-depth rule (combining O3 + S2). Gate: revive at 3+ chains for P3 family.
- Previous M4, M5, M9, N5, N6, O3-overlap, O4, O5/O6, R4 — gates unchanged.
- New patterns P5, P6, P7, P8 at 1 chain — gate: 3+ chains for cross-cutting.

### Always-On Baseline

- S8 (extended monitoring).

### Research Frontier (Not A Candidate)

- Q-RF Reinforcement (third specific instance: proxy-vs-structural-awareness).

## Innovation Telemetry

Generators applied: 4 / 4. Framers applied: 3 / 3.

Convergence: yes. Multiple mechanisms converge on per-discipline narrow patches at trigger-classifier-rule + prosecution-depth + differential-classifier layers, plus content cleanup, monitoring extension, N9 promotion, M6-refinement-S2 deferral, Q-RF reinforcement.

Survivors tested: 9 candidate designs (S1, S2, S3, S7, S8, N9 PROMOTION, M6-refinement-S2 DEFERRED, Q-RF Reinforcement, plus implicit M6 consolidation review at chain 7+).

Failure modes observed: none. Premature evaluation avoided. Single-mechanism trap avoided. Early frame lock avoided. Innovation without grounding avoided. Mechanism exhaustion not reached. Survival bias checked (S2 single-chain caveat; M6-refinement-S2 deferred; Q-RF stays Research Frontier).

Overall: PROCEED.
