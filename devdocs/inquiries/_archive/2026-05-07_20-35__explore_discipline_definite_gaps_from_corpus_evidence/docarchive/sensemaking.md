# Sensemaking: Explore Discipline - Definite Gaps From Corpus Evidence

## User Input

`devdocs/inquiries/2026-05-07_20-35__explore_discipline_definite_gaps_from_corpus_evidence/_branch.md` — what is for-sure missing from `homegrown/explore/references/explore.md` based on 7 LOOP_DIAGNOSE chain findings; generic / meta-discipline framing required; reject speculative additions.

## Initial Sense Version (SV1 — Baseline Understanding)

The exploration converged on TWO for-sure-missing items: (1) a TYPE-AWARE PROBE RULE that requires empirical probing of any candidate carrying a quantifiable claim before stabilization (chain 1 M2; reinforced by chain 6); (2) a CONTEXTUAL SURROUND PRE-SCAN RULE that requires the Coarse Scan to include the territory's contextual / structural surround layer before going deep on inquiry-specific objects (chain 2 N1). Speculative additions filtered out. Both rules need generic phrasing and need to sit at specific surfaces in explore.md.

## Phase 1 — Cognitive Anchor Extraction

### Constraints

1. **For-sure criterion.** Each proposed gap must have (a) at least one chain showing the prior loop FAILED because the rule was absent, AND (b) the corrected loop SUCCEEDED because the rule was present. Single-chain weak evidence is rejected.
2. **Generic / meta-discipline language.** Rules must use structural-exploration vocabulary (scan, signal, probe, resolution, frontier, confidence map, mode, layer); must NOT use project-specific terms (navigation, route memory, codebase paths).
3. **No speculation.** "Might be useful" rules are rejected. Only "definitely missing per evidence" rules pass.
4. **Where-it-sits requirement.** Each rule must specify WHERE in explore.md's existing structure it goes (which section, which failure mode, which process step).
5. **Existing structure preserved.** Rules extend explore.md's existing structure; no wholesale restructuring.

### Key Insights

1. **Two rules are CROSS-CHAIN evidenced.** M2 (probe-before-stabilization) is supported by chain 1 (primary case) and chain 6 (Exploration Cycle 4 reinforcement, same pattern in artifact mode). N1 (contextual-surround pre-scan) is supported by chain 2 (primary case). Both pass the for-sure criterion.

2. **The two rules target different existing failure modes.** M2 targets Surface-Only Scanning (the existing failure mode's prevention is "probe at least one signal" — too weak; M2 strengthens it to "probe quantifiable claims specifically"). N1 targets Premature Depth (the existing prevention is "complete at least one coarse scan before probing" — too weak; N1 strengthens it to "the coarse scan must include the surround layer when present").

3. **The current explore.md has within-cycle rules but lacks BETWEEN-LAYERS rules.** All 6 components and 6 failure modes operate at the within-cycle layer (within a single resolution, within a single scan-probe iteration). No rule addresses the BETWEEN-LAYERS dimension (e.g., territory has a surround layer that frames the inquiry-specific layer). Both M2 and N1 are between-layers rules — M2 differentiates types of claims; N1 differentiates layers of territory.

4. **Generic framings extend explore.md's vocabulary correctly.** Both rules can be expressed using existing terms (scan, probe, candidate, stabilized set, resolution, layer). Neither requires inventing new vocabulary.

5. **M2 generalizes to BOTH modes.** Original chain-1 wording said "possibility-mode candidates"; the generic version covers both artifact and possibility modes because chain 6's reinforcement was in artifact mode. Strengthening, not weakening.

6. **The for-sure criterion filters out 3 speculative additions** — provenance tracking, operation-shape stability checks, cross-discipline vocabulary tracking. None has multi-source convergent evidence at the Exploration level.

### Structural Points

1. **Existing explore.md surface:** 2 modes / 6 components / 7-step cycle / 3 convergence criteria / 6 failure modes / 4-step Execute section.

2. **Two for-sure-missing rules:**
   - **Rule 1 — Type-Aware Probe Rule:** sits at Probe section + Surface-Only Scanning prevention.
   - **Rule 2 — Contextual Surround Pre-Scan Rule:** sits at Scan section + Premature Depth prevention + Coarse Scan in Resolution Progression.

3. **Rule independence:** the two rules are orthogonal. Rule 1 is about WHEN to probe (claim type triggers empirical probe). Rule 2 is about WHAT to scan (territory layers determine breadth requirement). They can be implemented independently.

4. **Rule integration with existing structure:** both rules EXTEND existing failure-mode prevention rules (Surface-Only Scanning + Premature Depth) without replacing them. The existing rules stay; new rules add specificity.

### Foundational Principles

1. **The discipline is domain-agnostic;** therefore any rule added to explore.md must be domain-agnostic.
2. **The for-sure criterion is conservative;** a rule that is "useful but speculative" is rejected per the user's explicit instruction ("not 'might improve' but sth that is def missing").
3. **Between-layers rules are valid;** the existing explore.md treats territory as flat, but real territories have layers (within-territory + contextual surround). Rules that introduce layer-awareness are not contradictions but completions.
4. **Type-awareness is valid;** the existing explore.md treats probes as homogeneous, but real claims differ in epistemic shape (qualitative vs quantifiable). Rules that introduce type-awareness are not contradictions but refinements.

### Meaning-Nodes

1. **Type-Aware Probe** — the central concept of Rule 1; refers to differentiating empirical-required-probes from qualitative-acceptable-probes based on the candidate's claim type.
2. **Contextual Surround Layer** — the central concept of Rule 2; refers to the territorial layer that establishes meaning for inquiry-specific objects.
3. **Stabilized Candidate Set** — terminology from the existing Possibility Mode + chain-1 M2; what the rule prevents (premature stabilization without empirical probe).
4. **Coarse Scan Breadth** — terminology from the existing Resolution Progression + chain-2 N1; what the rule extends (breadth must include surround when present).
5. **Cross-Chain Evidence Convergence** — the methodological criterion; M2 has 2 chains (chain 1 + chain 6); N1 has 1 chain (chain 2) but with very strong primary-cause evidence.

## Sense Version 2 (SV2 — Anchor-Informed Understanding)

Anchor extraction sharpens the picture. The territory of "what's for-sure missing from explore.md" has two clearly-defined items: (1) a Type-Aware Probe Rule and (2) a Contextual Surround Pre-Scan Rule. Both have direct corpus evidence; both pass the generic-reframing test; both extend existing failure-mode prevention rules without contradicting them.

The two rules are orthogonal (different surfaces, different mechanisms) and can be implemented independently. The for-sure criterion is the load-bearing filter — it explicitly rejects speculative additions and demands multi-source convergence (which M2 has across chains 1+6, and which N1 has via chain 2's structural primary-cause evidence + the corrected loop's explicit fix).

Question note: N1 is single-chain primary evidence. Does it pass the for-sure criterion? Resolution: the criterion requires (a) prior failed because rule was absent AND (b) corrected succeeded because rule was present. Both conditions are met by chain 2 alone (the prior `docarchive/exploration.md` Coarse Scan list omitted the cultural-norm files; the corrected `docarchive/exploration.md` Cycle 1 explicitly cites them). Single-chain primary-cause evidence with explicit before/after contrast IS for-sure. The criterion does not require cross-chain count.

Shift from SV1: the two rules are now anchored, the where-it-sits is specified, and the for-sure criterion is operationalized.

## Phase 2 — Perspective Checking

### Technical / Logical Perspective

The two rules are mechanically clean:

- Rule 1 (Type-Aware Probe): "If candidate carries quantifiable claim X, run empirical probe of X before stabilizing." Boolean test (carries quantifiable claim? Y/N); deterministic action (probe; stabilize after).
- Rule 2 (Contextual Surround Pre-Scan): "If territory has surround layer S, include items from S in coarse scan before going deep on inquiry-specific objects." Boolean test (has surround layer? Y/N); deterministic action (scan surround; then scan inquiry-specific).

Both rules are observable / verifiable: a runner reading the output can check whether the rule fired (was a probe of the quantifiable claim performed? Were items from the surround layer scanned?).

NEW ANCHOR (Constraint): Rules must be observable in the discipline's output (the structural map). Implicit-only rules cannot be evaluation-gated.

### Human / User Perspective

The user's question is "what is for-sure missing." The user explicitly distinguishes "for sure" from "might." The user wants high-confidence, evidence-backed gaps.

The two rules are presented WITH evidence and WITH the rejection of speculation. The user gets exactly what they asked for: a small, definite list (two items) rather than a long speculative list.

The user also constrained the framing: "generic and meta defined bc this is a thinking discipine." The two rules pass this test (both reframed in structural-exploration vocabulary).

NEW ANCHOR (Key Insight): The user's criterion is a feature, not a constraint. By demanding "for-sure," the user ensures the spec changes are minimal and high-leverage rather than speculative bloat.

### Strategic / Long-term Perspective

If both rules are added to explore.md:

- **Long-term effect on Exploration discipline:** the discipline becomes more rigorous about TYPE (claim differentiation) and LAYER (territory differentiation). Two new dimensions become explicit.
- **Long-term effect on the corpus:** the patterns observed in chains 1, 2, 6 should NOT recur in future MVL+ runs. If they do, the rules need refinement; if they don't, the patches are validated.
- **Compositional effect:** the two rules pair with cross-cutting rules (M6, TP3) from the LOOP_DIAGNOSE corpus to form a defense-in-depth system.

The strategic concern: are the two rules sufficient? Cycle 4 of Exploration confirmed no other Exploration-specific rules have multi-source corpus evidence. So the strategic answer is: TWO is the right number for now; future chains may surface a third.

NEW ANCHOR (Foundational Principle): "Add rules with corpus evidence; defer speculative rules to future evidence." This is the LOOP_DIAGNOSE Step 5 + Step 6 guardrail applied to discipline-spec evolution.

### Risk / Failure Perspective

Risks of adding the two rules:

- **Risk: rules over-fire.** Rule 1 might fire on candidates whose "quantifiable claim" is trivial; Rule 2 might fire when the surround layer is absent or unclear. Mitigation: the rule wording includes explicit recognition triggers ("carries a quantifiable claim" — must be observable; "territory has surround layer" — must be identifiable).
- **Risk: rules become bureaucratic.** A runner might mechanically check "is there a quantifiable claim?" without understanding why. Mitigation: the rules cite the failure-mode prevention they extend (Surface-Only Scanning, Premature Depth) — the runner sees the rule in context.
- **Risk: rules conflict with existing rules.** Mitigation: cycles 2-3 verified the rules EXTEND existing failure-mode preventions without replacing them. No conflict.
- **Risk: rules are insufficient.** Mitigation: the for-sure criterion is the right filter for now; future chains will surface additional rules if the pattern continues.

NEW ANCHOR (Risk): Over-fire risk for both rules; mitigated by recognition-trigger specificity in the rule wording.

### Resource / Feasibility Perspective

Implementation cost:

- Rule 1 (Type-Aware Probe): ONE PARAGRAPH addition to explore.md. Sits in Probe section + Surface-Only Scanning prevention. Trivial.
- Rule 2 (Contextual Surround Pre-Scan): ONE PARAGRAPH addition. Sits in Scan section + Premature Depth prevention. Trivial.

Both rules are LOW cost; HIGH leverage. The cost-benefit ratio is excellent.

NEW ANCHOR (Foundational Principle): One-paragraph extensions are the right granularity for thinking-discipline-spec changes. They preserve the spec's readability while addressing observed gaps.

### Definitional / Consistency Perspective

Consistency check against existing explore.md:

- Rule 1's "carries a quantifiable claim" is observable in the existing Possibility Mode wording ("Each candidate is a 1-2 sentence description"); a quantifiable claim within that description is observable.
- Rule 1's "empirical probe" is consistent with the existing Probe section ("Goes deeper on one specific region; produces detailed structural knowledge").
- Rule 2's "territory has a contextual / structural surround layer" extends the existing "territory" concept; surround vs inquiry-specific is a layer differentiation that didn't exist in current explore.md.
- Rule 2's "Coarse Scan must include items from the surround layer" extends the existing Coarse Scan ("what major regions exist? unweighted").

CHECK: Does explore.md's stated purpose contradict the new rules? Stated purpose: "structural exploration ... mapping unknown territory through iterative scan-signal-probe cycles." Both rules are within this scope (they refine WHAT to scan and WHEN to probe, not WHETHER to do these). No contradiction.

CHECK: Does explore.md's existing Premature Depth definition contradict Rule 2? Premature Depth says: "Going deep on the first interesting signal before scanning broadly." Rule 2 says: "scan the surround layer before going deep on inquiry-specific objects." These are consistent — both name "broad before deep" — but Rule 2 specifies WHAT BREADTH means when there's a surround layer. Rule 2 is a refinement, not a contradiction.

CHECK: Does explore.md's existing Surface-Only Scanning definition contradict Rule 1? Surface-Only Scanning says: "After each scan, detect signals and probe at least one." Rule 1 says: "When candidate carries quantifiable claim, probe THAT candidate specifically." Rule 1 strengthens the existing "probe at least one" — it doesn't contradict but specifies WHICH ONE under certain conditions.

NEW ANCHOR (Foundational Principle): Both rules are extensions, not replacements. They add specificity to existing failure-mode preventions. The existing prevention rules stay valid in cases the new rules don't fire.

## Sense Version 3 (SV3 — Multi-Perspective Understanding)

Six perspectives reveal:

- **Logical/Technical** confirms the rules are deterministic and observable.
- **Human/User** confirms the rules satisfy the user's explicit "for-sure" criterion.
- **Strategic** confirms two is the right number based on corpus evidence; future chains may add a third.
- **Risk/Failure** identifies over-fire risk; mitigated by recognition-trigger specificity.
- **Resource/Feasibility** confirms one-paragraph cost is appropriate.
- **Definitional/Consistency** confirms both rules EXTEND existing structures without contradicting them.

The shift from SV2 is about RISK management (over-fire mitigation) and CONSISTENCY confirmation (extensions, not replacements). The two-rule structure remains stable.

## Phase 3 — Ambiguity Collapse

### Ambiguity 1: Should Rule 1 (Type-Aware Probe) cover BOTH artifact and possibility modes, or only possibility mode (per chain 1's original wording)?

Chain 1's M2 wording said "possibility-mode candidates." Chain 6's reinforcement (active artifact population scan) was in artifact mode. So the pattern occurs in BOTH modes.

**Strongest counter-interpretation:** keep the rule mode-specific (possibility-mode only) because chain 1 is the primary evidence and chain 1 was possibility mode. Adding artifact mode is a generalization beyond the primary evidence.

**Why the counter-interpretation fails (structural grounds):** chain 6's evidence (corrected loop's Cycle 4 ran an empirical probe of an artifact-population claim) is structurally identical to chain 1's M2 pattern — a quantifiable claim about how many things exist, dispatched qualitatively in the prior loop. The mode (artifact vs possibility) is not the determinant; the CLAIM TYPE (quantifiable) is. Restricting to one mode leaves the same pattern uncatchable in the other mode.

**Confidence:** HIGH (cross-chain evidence in two modes; the pattern is mode-agnostic).

**Resolution:** Rule 1 covers BOTH modes. The recognition trigger is "candidate carries a quantifiable claim" — the trigger fires regardless of mode.

**What is now fixed?** Rule 1 applies to artifact mode AND possibility mode.

**What is no longer allowed?** Mode-specific framings of Rule 1.

**What now depends on this choice?** The rule's wording and where-it-sits — Probe section is shared between modes; Surface-Only Scanning failure mode is shared between modes. Single-rule extension at both surfaces.

**What changed in the conceptual model?** Rule 1 is mode-agnostic; the trigger is claim-type, not mode.

### Ambiguity 2: Should Rule 2 (Contextual Surround Pre-Scan) be a NEW failure mode or an EXTENSION of Premature Depth?

Chain 2's N1 framing says: "Omitting these in a codebase-context Exploration is a Premature Depth signal." So the natural placement is as an extension of Premature Depth's prevention rule.

**Strongest counter-interpretation:** make it a 7th failure mode ("Layer-Blindness" or similar) because the surround-layer concept introduces a new dimension (between-layers vs within-layers) that Premature Depth doesn't address.

**Why the counter-interpretation fails (structural grounds):** Premature Depth's stated definition is "Going deep on the first interesting signal before scanning broadly." The surround-layer omission is a SHAPE of "not scanning broadly enough" — broadly here means "across the relevant breadth." Adding "include surround layer" is specifying what BREADTH means when there's a surround layer. The conceptual shape is consistent.

A new failure mode would be appropriate if the mechanism were structurally distinct from Premature Depth. But the mechanism IS the same (going deep before adequately broad). So extension is the right placement.

**Confidence:** MEDIUM-HIGH (consistent shape; one inferential step about whether mechanism matches).

**Resolution:** Rule 2 is an EXTENSION of Premature Depth's prevention rule, not a new failure mode.

**What is now fixed?** Rule 2 sits at Scan section + Premature Depth's "How to prevent" + Coarse Scan in Resolution Progression. No new failure mode introduced.

**What is no longer allowed?** Treating Rule 2 as a 7th failure mode.

**What now depends on this choice?** Rule 2's wording references Premature Depth explicitly ("a Premature Depth instance applied at the layer-of-scan dimension").

**What changed in the conceptual model?** explore.md's failure-mode count stays at 6.

### Ambiguity 3: Are there really only TWO for-sure-missing rules, or did Cycle 4 miss something?

Cycle 4 scanned chains 3-7 for Exploration-related shortcomings and found none at the primary-cause level. Speculative additions (provenance tracking, operation-shape stability) were filtered out.

**Strongest counter-interpretation:** Cycle 4 was too quick; chain 6's "discovery-mechanism gap" is partly Exploration's responsibility (the corrected loop's Cycle 4 ran the active artifact population scan; the prior didn't). This could be a third gap.

**Why the counter-interpretation fails (structural grounds):** chain 6 itself attributes the gap to Sensemaking specification gap (Stage A) and Decomposition missing discovery-mechanism piece (Stage B), not to Exploration. The corrected loop's Exploration Cycle 4 IS evidence — but it's evidence of the SAME pattern as chain 1's M2 (probe a quantifiable claim before stabilizing). It is not a distinct third gap; it is reinforcement of the M2 pattern in artifact mode.

**Confidence:** HIGH (the chain-6 finding's primary-cause attribution is explicit; the Cycle-4 evidence is reinforcement of the M2 pattern, not a new pattern).

**Resolution:** TWO for-sure-missing rules. Chain 6 reinforces Rule 1 (M2), not a third rule.

**What is now fixed?** The for-sure-missing inventory has 2 items.

**What is no longer allowed?** Adding a third rule from chain 6 alone (chain 6's primary cause is Sensemaking + Decomposition, not Exploration).

**What now depends on this choice?** The diagnostic verdict and the maintenance candidate set — TWO rules, not three or more.

**What changed in the conceptual model?** Final inventory of for-sure-missing items is fixed at 2.

### Ambiguity 4: Should Rule 1's wording cover ALL quantifiable claim types (cost, benefit, frequency, magnitude, count) or only some?

Chain 1's M2 wording said "quantifiable cost or benefit." Chain 6's evidence was a count claim (how many `PastNavigationMemoryFile` candidates exist). So at least three types are evidenced (cost, benefit, count).

**Strongest counter-interpretation:** narrow Rule 1 to only the explicitly evidenced types (cost, benefit, count) to avoid scope creep. Other types (frequency, magnitude) are not evidenced.

**Why the counter-interpretation fails (structural grounds):** the underlying mechanism is "quantifiable claim → empirical probe required." The evidenced types are SUB-TYPES of "quantifiable." Narrowing to only the evidenced sub-types creates artificial gaps; future chains may surface frequency/magnitude/etc. and the rule would need refinement each time.

A more honest framing: enumerate the types as EXAMPLES, not as an exhaustive list. The rule fires on "any quantifiable claim"; the example list (cost, benefit, frequency, magnitude, count) is illustrative.

**Confidence:** MEDIUM-HIGH.

**Resolution:** Rule 1 covers ALL quantifiable claim types. The example list is illustrative; the trigger is "carries a quantifiable claim."

**What is now fixed?** Rule 1 wording uses "quantifiable claim" as the trigger; the type list is illustrative.

**What is no longer allowed?** Narrow type-specific rule wordings.

**What now depends on this choice?** Rule 1's evaluation gate — observable as "if a candidate's text contains a quantifiable claim (X amount, Y per Z, N items, etc.), the probe must address that claim."

**What changed in the conceptual model?** Rule 1's scope is broader but evidence-grounded; the rule covers the underlying mechanism, not just the evidenced sub-types.

## Sense Version 4 (SV4 — Clarified Understanding)

After ambiguity collapse:

- **TWO rules** are for-sure-missing (no more, no fewer).
- **Rule 1** (Type-Aware Probe) covers BOTH artifact and possibility modes; trigger is "carries a quantifiable claim" of any type (cost, benefit, frequency, magnitude, count, etc.).
- **Rule 2** (Contextual Surround Pre-Scan) is an EXTENSION of Premature Depth, not a new failure mode.
- **Both rules** sit at specific surfaces in explore.md (Probe section + Surface-Only Scanning prevention for Rule 1; Scan section + Premature Depth prevention + Coarse Scan in Resolution Progression for Rule 2).
- **Speculative additions filtered out:** provenance tracking, operation-shape stability checks, vocabulary tracking — all rejected.

## Phase 4 — Degrees-of-Freedom Reduction

### What Variables Are Now Fixed

1. **TWO for-sure-missing rules.** No more, no fewer.
2. **Rule 1 = Type-Aware Probe Rule.** Covers both modes; trigger is "carries a quantifiable claim."
3. **Rule 2 = Contextual Surround Pre-Scan Rule.** Extension of Premature Depth; not a new failure mode.
4. **Rule 1 sits at Probe section + Surface-Only Scanning prevention.**
5. **Rule 2 sits at Scan section + Premature Depth prevention + Coarse Scan in Resolution Progression.**
6. **Both rules use generic / meta-discipline language.**
7. **Both rules extend existing structures; do not contradict.**
8. **Both rules are one-paragraph in size.**

### What Options Are Eliminated

1. **NO third (or more) rule** from single-chain non-convergent evidence.
2. **NO new failure mode** for Rule 2 (extension only).
3. **NO mode-specific framing** for Rule 1 (cover both modes).
4. **NO project-specific wording** in either rule.
5. **NO speculative additions** (provenance, operation-shape, vocabulary).
6. **NO wholesale restructuring** of explore.md.

### What Paths Remain Viable

1. **Rule 1 wording variations** (Generic / Focused / Contrarian per /innovate guidance).
2. **Rule 2 wording variations** (Generic / Focused / Contrarian).
3. **Where-it-sits placement decisions** (one section vs both sections; Probe + failure-mode prevention; etc.).
4. **Evaluation gate specifications** (how to test whether the rule fires correctly).
5. **Recognition-trigger specifications** (how to identify when the rule should fire).

## Sense Version 5 (SV5 — Constrained Understanding)

The constrained solution space:

- **Two rules** with stable structure.
- **Rule 1** = Type-Aware Probe Rule; fires when candidate carries quantifiable claim; covers both modes; sits at Probe + Surface-Only Scanning prevention.
- **Rule 2** = Contextual Surround Pre-Scan Rule; fires when territory has surround layer; extension of Premature Depth; sits at Scan + Premature Depth prevention + Coarse Scan.
- **Both rules** are one-paragraph generic/meta-discipline extensions, not project-specific patches.
- **Innovation's job** is to generate concrete wording variations.
- **Critique's job** is to test each rule against the for-sure criterion (definitely missing + evidenced + multi-source convergence + generic phrasing).

## Phase 5 — Conceptual Stabilization

### Stable Model

```
For-sure missing from homegrown/explore/references/explore.md (2 rules):

RULE 1 — Type-Aware Probe Rule
  Where: Probe section + Surface-Only Scanning prevention extension
  Trigger: "Candidate carries a quantifiable claim (cost, benefit, frequency,
           magnitude, count, etc.)"
  Action: "Run at least one empirical probe of the quantifiable claim before
           the candidate can pass into the stabilized candidate set."
  Evidence: chain 1 (M2; primary cause; HIGH); chain 6 (reinforcement;
            artifact-mode pattern of same shape)
  Generic vocabulary: scan, candidate, quantifiable claim, empirical probe,
                       stabilized candidate set
  Failure-mode tie: Surface-Only Scanning (existing prevention "probe at
                     least one signal" → "probe quantifiable claims
                     specifically")

RULE 2 — Contextual Surround Pre-Scan Rule
  Where: Scan section + Premature Depth prevention extension + Coarse Scan
         in Resolution Progression
  Trigger: "Territory has a contextual / structural surround layer that
            establishes meaning for inquiry-specific objects"
  Action: "Coarse Scan must include items from the surround layer before
           going deep on inquiry-specific objects."
  Evidence: chain 2 (N1; primary cause; HIGH; explicit before/after
            contrast in prior + corrected exploration outputs)
  Generic vocabulary: territory, contextual surround layer, coarse scan,
                       inquiry-specific objects
  Failure-mode tie: Premature Depth (existing prevention "complete at least
                     one coarse scan before probing" → "the coarse scan
                     must include the surround layer when present")
```

### Action Framework

For Decomposition / Innovation / Critique:

```
Decomposition:
  Partition the 2 rules into independently-implementable pieces.
  Map: which rules interact? (Answer: orthogonal, no interaction.)
  Specify dependencies: none between Rule 1 and Rule 2.

Innovation:
  Generate 3 variations per rule (Generic / Focused / Contrarian).
  Run all 7 mechanisms.
  Assembly check: do the rules combine into something stronger?

Critique:
  Apply for-sure criterion (definitely missing + evidenced + generic + extends-existing-structure).
  Verdict per rule: SURVIVE / REFINE / KILL.
  Reject any speculative additions Innovation surfaces.
```

## Final Sense Version (SV6 — Stabilized Model)

The corpus evidence converges on TWO for-sure-missing rules in `homegrown/explore/references/explore.md`:

1. **Type-Aware Probe Rule** — when a candidate carries a quantifiable claim, an empirical probe of that claim is required before the candidate can pass into the stabilized candidate set. This rule sits at the Probe section + Surface-Only Scanning failure-mode prevention. It applies to BOTH artifact and possibility modes. It is supported by chain 1 (primary cause; M2 candidate verbatim) and chain 6 (artifact-mode reinforcement via the corrected loop's Cycle 4 active artifact population scan).

2. **Contextual Surround Pre-Scan Rule** — when the territory has a contextual / structural surround layer (a layer that establishes meaning for inquiry-specific objects within it), the Coarse Scan must include items from that surround layer before going deep on inquiry-specific objects. This rule sits at the Scan section + Premature Depth failure-mode prevention + Coarse Scan in Resolution Progression. It is supported by chain 2 (primary cause; N1 candidate verbatim) with explicit before/after contrast in the prior and corrected exploration outputs.

Both rules are EXTENSIONS of existing failure-mode preventions, not new failure modes or wholesale restructurings. Both use generic / meta-discipline vocabulary fit for a domain-agnostic thinking discipline. Both have HIGH confidence per the for-sure criterion (definitely missing + evidenced + multi-source convergence for Rule 1; primary-cause + before/after contrast for Rule 2 even at single chain).

Speculative additions (provenance tracking, operation-shape stability checks, vocabulary tracking) are rejected because they lack multi-source corpus evidence at the Exploration level.

Verdict (preliminary, pending Critique): TWO for-sure-missing rules to add to explore.md. Each is one-paragraph in size, generic in framing, and extends existing structures without contradicting them.

The shift from SV1 is: two rules anchored, where-it-sits specified, mode-coverage and failure-mode-tie resolved, speculative additions filtered. The conceptual structure is stable and ready for Decomposition's piece partitioning and Innovation's wording variations.
